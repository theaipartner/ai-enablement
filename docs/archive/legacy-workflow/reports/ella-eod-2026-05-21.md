# Report: Ella 2026-05-21 EOD State Entry
**Slug:** ella-eod-2026-05-21
**Spec:** docs/specs/ella-eod-2026-05-21.md

## Files touched

**Modified:**
- `docs/state.md` — new `### 2026-05-21 EOD — Ella misfire resolution arc + production resume` entry inserted at the top of the `## Gregory editorial skin shipped` section (above the four existing 2026-05-21 per-ship entries). Covers the seven content requirements from the spec (arc narrative, production posture, day's ships with pointers, open issues, gate (c) pending, monitoring queries, running tallies).
- `docs/known-issues.md` — new short entry titled "Unanswered-flagger Slack permalinks unfurl only when Ella's bot is in the source channel" inserted above the existing `author_type='bot'` entry. Four-line format (What / Why it matters / Next action / Logged) matching the file's documented entry shape. Explains the Slack-side unfurl behavior, points to `scripts/invite_ella_and_bot_to_client_channels.py` as the relevant ops rollout tool.
- `docs/specs/ella-eod-2026-05-21.md` — `Status:` flipped from `in-flight` to `shipped` as part of the report commit per CLAUDE.md § Spec and report convention.

**Created:**
- `docs/reports/ella-eod-2026-05-21.md` — this report.

No new files outside the report. No code, no tests, no migrations, no env-var changes. No production data touched.

## What I did, in plain English

The spec was a pure docs-only EOD entry covering a non-linear day: prior fix landed → resumed → still misfiring → diagnosed across wrong hypotheses → root-caused (`message_changed` outer-ts) → fixed → validated → resumed again. Acclimatized by reading state.md (the `## Gregory editorial skin shipped` section to confirm the reverse-chronological convention and how the four existing 2026-05-21 entries are framed), known-issues.md (entry format + existing `author_type='bot'` and `Code hygiene — deferred cleanup` entries), and a couple of the underlying reports (the dedup-message-changed report and the unanswered-flagger report) to confirm the test count, migration count, and serverless-function count.

Confirmed in four bullets:
- State.md entries under `## Gregory editorial skin shipped` are reverse-chronological. The EOD entry goes at the top of the section, above the existing per-ship 2026-05-21 entries — narrative connector for the day, framed differently from the per-ship entries.
- Test count = 706 (from the doc-hygiene-sweep entry already in state.md line 43), migrations = 42, Python serverless functions = 13, TopNav tabs = 6. All unchanged today (docs-only).
- `author_type='bot'` finding is already in known-issues.md (line 73, logged 2026-05-21). I reference it from the EOD entry rather than re-logging.
- The permalink-unfurl finding is NOT logged anywhere yet. The spec's "one judgment call for Builder" leans option (b) (also log to known-issues, not just in the EOD entry). I went with (b) — operational gotchas surface better in known-issues than buried in a single-day EOD entry.

The EOD entry covers the seven content requirements from the spec § "What the EOD entry must contain":

1. **Arc narrative** — six paragraphs walking the day chronologically. Wrong hypotheses each get one sentence (per hard stop #4 — tight, not an essay). The query-construction error (`slack_channel_id` vs `triggering_slack_channel_id` key) gets one sentence as part of the diagnostic detour paragraph. The wrong-call acknowledgment of the prior idempotency spec's "acceptable for v1" assumption is owned in a dedicated paragraph.
2. **Current production posture** — 136 channels at `passive_monitoring_enabled=true`, kill switches as fallback, `#ella-test-drakeonly` as validation surface.
3. **Day's ships** — six bullet pointers (today's four + the two recap items from 2026-05-20 that are part of the same arc). Slugs match the actual spec/report filenames.
4. **Open issues** — three bullets: `author_type='bot'` (existing known-issues entry referenced), the permalink-unfurl finding (new known-issues entry referenced), the tabled architectural-rehaul conversation with the four product-shape options.
5. **Gate (c) still pending** — unanswered-flagger team_member-exclusion smoke case.
6. **Monitoring queries** — two SQL blocks: a `dupes_caught` query against `webhook_deliveries.processing_status='duplicate'` and a `haiku_decision`-mix query against `agent_runs`. Reconstructed from the table shapes (the audit-row contract documented in the dedup-message-changed entry; the `trigger_metadata.haiku_decision` field per the passive-monitor design). Includes a comment to adjust the timestamp cutover to the actual UTC second-resume time.
7. **Running tallies** — 42 migrations, 13 Python serverless functions, 706 pytest, 6 TopNav tabs — all unchanged.

Sequenced as two commits:
- **Commit 1** added the EOD entry + the unfurl known-issues entry (one logical change: today's doc-only EOD ship).
- **Commit 2** (this report + spec status flip) is the standard report-and-flip closer per CLAUDE.md § Spec and report convention.

## Verification

**Hard stops verified:**
- **#1 (documentation only):** held. No code, tests, migrations, or env-var changes. `git diff --stat` post-commit-1 shows only `docs/known-issues.md`, `docs/specs/ella-eod-2026-05-21.md`, `docs/state.md`.
- **#2 (don't contradict the arc timeline):** held. Cross-checked the dedup-message-changed report's "11 duplicate dispatches / 8 channels / 36 hours" framing against the spec's account; matched. The wrong-key query-construction error and the `author_type='bot'` side-finding are both surfaced in the diagnostic report's Surprises section per the spec.
- **#3 (no retroactive editing of prior state.md entries):** held. The four 2026-05-21 per-ship entries are untouched; the EOD entry is purely additive at the top of the section.
- **#4 (keep it tight):** the arc narrative is ~6 paragraphs; wrong hypotheses get one sentence each. The full entry runs ~70 lines including the SQL monitoring queries — readable in under two minutes per spec target.
- **#5 (test suite not affected):** held by construction. `pytest tests/` stays at 706 (the existing baseline from the hygiene-sweep entry). `tsc --noEmit` and `next lint` stay clean by construction.

**Drake gates verified:**
- (a) None — no migrations, no irreversible actions. ✓
- (b) The arc timeline matched what's in the underlying specs/reports (no contradictions surfaced). The one prior-spec judgment error the EOD entry calls out (idempotency-spec's "acceptable for v1" assumption that production reality contradicted) is consistent with the dedup-message-changed report's own Surprises section. ✓
- (c) None — no behavior to validate. ✓
- (d) None. ✓

**Done-means checklist (from spec § Done means):**
- New 2026-05-21 EOD entry in `docs/state.md` covering all seven content requirements. ✓
- Option (b) chosen for the unfurl finding; short known-issues entry added. ✓
- All gates green at prior counts (no code touched). ✓
- Spec status flipped to `shipped` in this report commit-sequence. ✓
- Report at `docs/reports/ella-eod-2026-05-21.md` follows the 6-section structure (this file) and notes the judgment call on the unfurl finding (option b). ✓

## Surprises and judgment calls

- **Judgment call on the unfurl finding (option b chosen).** The spec leans (b) and gives Builder discretion. I went with (b). Reasoning: operational gotchas where someone will spend time investigating ("why isn't the permalink expanding?") are exactly what known-issues exists for. The EOD entry references it; the known-issues entry holds the operational detail (next-action points to `scripts/invite_ella_and_bot_to_client_channels.py` as the existing rollout tool). The known-issues entry is short — four lines per the file's documented format — so the cost of adding it is low.

- **Monitoring queries reconstructed, not lifted verbatim.** The spec said "Builder pulls the exact SQL from the working session's record if available, or reconstructs it from the table shapes." No session-record SQL was in scope to pull from this session, so I reconstructed both queries from the documented table shapes: `webhook_deliveries.processing_status='duplicate'` per the dedup-message-changed entry's audit-row contract; `agent_runs.trigger_metadata->>'haiku_decision'` per the passive-monitor design described in state.md's Batch 2.3 entry. The timestamp cutover is parameterized via an inline `-- adjust` comment because the exact second-resume UTC time isn't in the spec's source material.

- **The arc's "second resume" UPDATE qualifies on `passive_monitoring_enabled = false`, not just `test_mode = false`.** The first resume's UPDATE was unconditional on `test_mode = false`. The second resume's UPDATE adds an `AND passive_monitoring_enabled = false AND is_archived = false` qualifier per the spec's verbatim quote — narrower scope so it doesn't disturb channels already-on (e.g., `#ella-test-drakeonly`). I preserved this exactly in the EOD entry.

- **Spec status flip lives in the report commit, not the EOD-content commit.** The two are conceptually separate: commit 1 is "the doc-only EOD ship," commit 2 is "the report + the spec-shipped flag." CLAUDE.md § Spec and report convention says the flip rides with the report commit, which is what I did.

- **The wrong-hypothesis paragraphs were the hardest editorial call.** Tempting to give each one paragraph for clarity, but the spec is explicit ("Wrong hypotheses get one sentence each, not paragraphs"). Kept to one-sentence-each per hypothesis, bundled into the diagnostic-detour paragraph. The query-construction error is the third sentence in that bundle.

## Out of scope / deferred

- **The architectural rehaul conversation.** Surfaced in the EOD entry's Open issues as a tabled item with the four product-shape options. Per spec, this stays a flag for a future conversation (likely with Nabeel) — no decision in scope today.
- **The `author_type='bot'` parser fix.** Surfaced in the EOD entry as an open issue with pointer to the existing known-issues entry. Spec for a future session per that entry's Next action.
- **Full-fleet rollout of Ella bot-channel-membership.** The new permalink-unfurl known-issues entry points to the existing `scripts/invite_ella_and_bot_to_client_channels.py` as the rollout tool but doesn't run it — operational, Drake-led work.
- **Gate (c) confirmation on the unanswered-flagger team_member-exclusion case.** Drake's organic observation per the underlying spec — not in scope here. Referenced in the EOD entry's "still pending gate (c)" section.

## Side effects

None. Pure documentation-only change. No Slack posts, no emails, no shared-DB writes, no external API calls, no file creations outside the repo. The two commits will fire Vercel's GitHub-integration auto-deploy as a side effect, but since no code changed the build produces an identical bundle — no behavioral change in production. No production traffic generated by this work.
