# Live System State

Historical state record. **Read on demand** when a spec or task references recent shipped subsystems, or when chat needs historical detail. Not auto-loaded per session — CLAUDE.md is the always-loaded surface; this file is the lookup.

Update discipline: the spec that ships a batch's code also updates this file, in the same Builder commit-sequence. Same pattern as CLAUDE.md § Live System State used to follow before the 2026-05-11 split. If this file drifts from reality, it becomes a doc nobody trusts.

---

As of 2026-05-08 (Call Review V1 + Gregory V2 brain + Fathom auto-review + daily cron + freshness filter + NPS-is-gospel + latest_nps source fix + clients list V2 columns + journey stage taxonomy + default-collapse Financials/Profile sections all shipped):

- **Cloud Supabase** is the production target. Project ref `sjjovsjcfffrftnraocu` (region us-east-2, Ohio). **36 migrations applied** (`0001_core_entities` through `0036_director_tasks`). Latest: 0036 (`director_tasks`) — single-table addition for the Director task page at `/tasks` (creator-tier gated). FK CASCADE on `team_members`; per-user `(team_member_id, created_at DESC)` index for the page query. Hard-delete via the dashboard's delete button. Spec: `docs/specs/director-tasks-and-list-ux-polish.md`. 0035 (`nabeel_remove_from_csms`) — one-row data UPDATE flipping `team_members.is_csm` from true to false for Nabeel Junaid, who no longer takes client calls (2026-05-15). His existing `client_team_assignments` rows stay intact per Drake's direction. Spec: `docs/specs/teams-personal-email-exclusion-and-nabeel-removal.md`. 0033 (`oauth_tokens`) + 0034 (`calendar_events`) — two-table addition for the Teams Meeting Tracker (2026-05-14). 0033 stores per-team-member OAuth credentials with provider CHECK pinned to `'google'` + a unique index on `(team_member_id, provider)` as the upsert key. 0034 caches Google Calendar events with a unique index on `(team_member_id, google_event_id)` + a secondary index on `(team_member_id, start_time)` for the per-week page query. Both dual-verified post-apply; public table count 23 → 25 as expected. Spec: `docs/specs/teams-meeting-tracker.md`. 0032 (`team_members_access_tier`) added `team_members.access_tier text NOT NULL DEFAULT 'csm'` + CHECK pinning the four values `csm`/`head_csm`/`admin`/`creator` + backfill of the 6 known users (Drake creator, Nabeel admin, Scott Wilson head_csm, Lou/Nico/Zain csm explicit; everyone else stays at default csm). Drake's email confirmed via SELECT pre-apply (`drake@theaipartner.io`, not the gmail in the spec draft). Dual-verified post-apply: column present, CHECK constraint exists, ledger has the version, 3 rows elevated + 8 at default = 11 total, no public-table drift. Spec: `docs/specs/permissions-access-tiers.md`. 0031 (`slack_channels_test_mode`) added `slack_channels.test_mode boolean NOT NULL DEFAULT false` + inline UPDATE enabling it on `#ella-test-drakeonly` so Drake can smoke-test passive monitoring as a team_member (Batch 2.3 follow-up, 2026-05-11; dual-verified post-apply — column present, exactly 1 row flipped to true, 136 other rows stay false). 0029 (`rename_ella_enabled_to_passive_monitoring`) renamed `slack_channels.ella_enabled` to `passive_monitoring_enabled` + reposted the partial index under the new name + atomically reissued the `create_or_update_client_from_onboarding` RPC with the new column name in its Branch C INSERT (Batch 2.3, 2026-05-11). 0030 (`pending_ella_responses`) created the queue table for Ella V2 Batch 2.3 passive monitoring with the haiku_decision + status CHECK enums + the partial `pending_ella_responses_due_idx` on queued rows. Recent migrations: 0017 added 14 columns to `clients` + 1 column to `nps_submissions` + 4 history/upsell tables (M4 Chunk A). 0018 added 4 `security definer` Postgres functions for atomic update + history-row writes (M4 Chunk B2). 0019 (`status_add_leave`) added the first DB-level CHECK on `clients.status` and expanded the vocabulary to include `leave` (M5.3). 0020 (`trustpilot_rename_vocab`) renamed `clients.trustpilot_status` 1:1 to match Scott's master sheet (M5.3b). 0021 (`nps_standing_and_gregory_bot`) added `clients.nps_standing` + Gregory Bot sentinel team_member (UUID `cfcea32a-062d-4269-ae0f-959adac8f597`) + `update_client_from_nps_segment` RPC (M5.4 Path 1). 0022 (`status_cascade`) added `clients.accountability_enabled` + `clients.nps_enabled` + `team_members.is_csm` + Scott Chasing sentinel (UUID `ccea0921-7fc1-4375-bcc7-1ab91733be73`) + BEFORE/AFTER triggers for the negative-status cascade (M5.6). 0023 (`change_primary_csm_on_conflict`) replaced the 0014 RPC with an `ON CONFLICT DO UPDATE` variant (M5.6 hotfix). 0024 (`trustpilot_cascade_on_happy`) added a one-directional BEFORE UPDATE trigger that auto-flips `clients.trustpilot_status` to `'ask'` when `csm_standing` transitions to `'happy'` (M5.7). 0025 (`create_or_update_client_from_onboarding`) added the security-definer RPC for Path 3 inbound (M5.9). 0026 (`onboarding_webhook_optional_slack`) made phone / slack_user_id / slack_channel_id optional on the Path 3 RPC (M6.x — supports Zain's two-pass onboarding flow). 0027 (`nps_is_gospel`) flipped `update_client_from_nps_segment` from override-sticky to always-auto-derive csm_standing from segment + one-time backfill of 16 stale rows (2026-05-08). 0028 (`journey_stage_check`) added a CHECK constraint on `clients.journey_stage` pinning the six-value funnel taxonomy (2026-05-08). Migrations 0001–0010 applied via the CLI; 0011–0028 applied via Studio + manual ledger insert during the CLI-broken era (2026-04-28 to 2026-05-08, see `docs/known-issues.md` § resolved entries); migration 0029 onward uses the CLI again per `docs/runbooks/apply_migrations.md` post-Phase-3 (psql not installed in this environment, so dual-verify uses psycopg2 against the pooler URL — discipline held throughout). Accessed via the pooler URL stored in `supabase/.temp/pooler-url`; the DB password lives in `.env.local` as `SUPABASE_DB_PASSWORD` (quoted because it contains a `#`).
- **Vercel deployment** live at `https://ai-enablement-sigma.vercel.app`. Single project, mixed-framework: Next.js 14 dashboard at repo root + **ten** Python serverless functions in `api/`. `vercel.json` declares `"framework": "nextjs"` plus per-file Python runtimes for: `api/slack_events.py` (Ella's Slack handler, `maxDuration: 60`), `api/fathom_events.py` (Fathom webhook, `maxDuration: 60`), `api/fathom_backfill.py` (daily cron, `maxDuration: 300`), `api/gregory_brain_cron.py` (daily cron, `maxDuration: 300`), `api/airtable_nps_webhook.py` (Path 1 inbound, `maxDuration: 60`), `api/accountability_roster.py` (Path 2 outbound GET, `maxDuration: 60`), `api/airtable_onboarding_webhook.py` (Path 3 inbound, `maxDuration: 60`), `api/accountability_notification_cron.py` (daily 7am EST CS-visibility cron, `maxDuration: 60` — added M6.1), `api/passive_ella_cron.py` (per-minute Ella passive-monitor queue drainer, `maxDuration: 60` — added Batch 2.3); `api/teams_calendar_sync_cron.py` (every-30-min Google Calendar sync, `maxDuration: 60` — added 2026-05-14 with the Teams Meeting Tracker). Vercel Cron schedules: `0 8 * * *` daily → `/api/fathom_backfill`; `0 9 * * *` daily → `/api/gregory_brain_cron` (switched from weekly to daily 2026-05-08, paired with the freshness filter on `ai_call_signal` so each sweep only fires Sonnet for clients with new call_reviews since their last compute); `0 12 * * *` daily → `/api/accountability_notification_cron`; `* * * * *` per-minute → `/api/passive_ella_cron`; `*/30 * * * *` every 30 min → `/api/teams_calendar_sync_cron`. Env vars in production: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `SLACK_BOT_TOKEN`, `SLACK_SIGNING_SECRET`, `SLACK_USER_TOKEN`, `FATHOM_WEBHOOK_SECRET`, `FATHOM_API_KEY`, `CRON_SECRET` (validated by all cron endpoints — fathom_backfill, gregory_brain_cron, accountability_notification_cron, passive_ella_cron; consolidated to single-var pattern in M6.2), `AIRTABLE_NPS_WEBHOOK_SECRET`, `MAKE_OUTBOUND_ROSTER_SECRET`, `AIRTABLE_ONBOARDING_WEBHOOK_SECRET`, `SLACK_CS_CALL_SUMMARIES_CHANNEL_ID` (M6.1), `SLACK_CS_ACCOUNTABILITY_CHANNEL_ID` (M6.1), `AIRTABLE_ACCOUNTABILITY_PAT` (M6.1), `AIRTABLE_ACCOUNTABILITY_BASE_ID` (M6.1), `AIRTABLE_ACCOUNTABILITY_TABLE_ID` (M6.1), `ELLA_PASSIVE_MONITORING_ENABLED` (Batch 2.3 global kill switch — must be `'true'` for any passive behavior; default unset = off), `ELLA_PASSIVE_KB_RELEVANCE_THRESHOLD` (Batch 2.3 optional, default 0.3), `ESCALATION_RECIPIENT_SLACK_USER_ID` (2026-05-14 — Slack user_id of the head CSM Scott DMed alongside the primary CSM on every Ella escalation; unset → primary CSM only), `GOOGLE_OAUTH_CLIENT_ID` + `GOOGLE_OAUTH_CLIENT_SECRET` (2026-05-14, Teams Meeting Tracker — Google Cloud Console OAuth 2.0 Web client; Calendar API readonly scope), `NEXT_PUBLIC_APP_URL` (2026-05-14, Teams Meeting Tracker — base URL for the OAuth redirect URI registered in Google Cloud Console). (`GREGORY_CONCERNS_ENABLED` was the V1.1 concerns gate — retired in V2 brain ship 2026-05-07; if still set in Vercel it's a no-op.)
- **Gregory dashboard** live with the V1 client page schema (M4) + M5 vocab updates + M5.5 filter bar + M5.6 cascade + M5.7 monthly-meetings/inactivity/country/accountability/NPS-toggle filters. 188 non-archived clients post-M5 cleanup, perfect 1:1 with the canonical master sheet.
- **`clients` table population (post-cleanup, 2026-05-05):** 188 non-archived clients. Every negative-status client has `csm_standing='at_risk'` + `accountability_enabled=false` + `nps_enabled=false` (M5.6 cascade); every active client has the toggles at default `true`. `country` populated USA/AUS for every CSV-matched client. `clients.nps_standing` populated for 61 active clients via Path 1 backfill + alternate-emails resync.
- **Path 2 outbound roster live count:** 128 actionable / 188 non-archived (60 filtered for missing slack_user_id / channel / email). Surfaces a Slack-identity coverage gap; followup logged.
- **CS visibility surfaces (M6.1):** `agents/gregory/cs_call_summary_post.py` hooks into `ingestion/fathom/pipeline.py:ingest_call` for client-category calls (audit trail via `webhook_deliveries.source='cs_call_summary_slack_post'`). `api/accountability_notification_cron.py` runs daily at 12:00 UTC (`webhook_deliveries.source='accountability_notification_cron'`); 91 active accountability-enabled clients in scope at ship, all with active primary_csm assignments (the cron's no-CSM silent-drop bucket is empty today). Two new harnesses: `scripts/test_cs_call_summary_locally.py` (28/28) and `scripts/test_accountability_notification_cron_locally.py` (31/31). Slack-post infrastructure factored to `shared/slack_post.py` (Ella's two-token logic stays in `api/slack_events.py` and imports the transport from there; 14/14 Ella post-tests still pass after the refactor). **2026-05-11:** Slack post format tightened to sentiment-only — `_format_review_message` now renders just the `**Sentiment**` section + `View in Gregory` deep link; Pain points / Wins / Conversation pivots dropped (CSMs click through for the rest). `_format_review_items` helper deleted as dead code. Skip-when-sentiment-missing contract unchanged. Harness 63/63 green; test 11 rescoped to sentiment-only mrkdwn cleanup (no coverage loss — rogue `**bold**` and `### header` Markdown still exercised via `sentiment_arc`).
- **Call Review V1 (2026-05-07).** New `call_reviewer` agent at `agents/call_reviewer/` (Sonnet-only, system prompt + `review_call` + `upsert_call_review`). Generates a four-section structured review per call (pain_points / wins / dodged_questions / sentiment_arc) stored as `documents` rows with `source='fathom'`, `document_type='call_review'`, `is_active=False` (retrieval-side safety net — never lands in `match_document_chunks` results). May 2026 backfill complete: 31/31 reviewed, $1.5349 total Sonnet cost. One-shot script at `scripts/backfill_call_reviews.py` with `--smoke` / `--apply` / `--limit` modes; smoke mode is the working norm for all future backfills (see CLAUDE.md § Working Norms § Operational patterns). Calls detail page surfaces the review as Section 4.5 between Summary and Action items — sentiment_arc paragraph + three list subsections + "Generated <timestamp>" header. The "Conversation pivots" subsection renders the underlying `dodged_questions` data (renamed user-facing only in V2 brain ship).
- **Fathom pipeline auto-review (2026-05-07).** `ingestion/fathom/pipeline.py:_ensure_call_review_document` fires after each successful summary write for client-category calls with a non-null `primary_client_id`. Three-layer idempotency (existence guard + persistence upsert + pipeline invariant) keeps Fathom retries free. Fail-soft try/except — review-generation failure never breaks Fathom delivery. The 30-day call_review lookback window the V2 brain reads now refills automatically as calls land. `review_call` gained optional `trigger_type` kwarg (default `manual_backfill`; pipeline passes `fathom_pipeline`).
- **Gregory brain V2 (2026-05-07).** New `ai_call_signal` at `agents/gregory/ai_call_signal.py` is the dominant contributor (weight **0.50**) — reads each client's last 30 days of call_review documents, sends to Sonnet, returns a 0-100 contribution + 1-3 sentence reasoning + 0-3 concerns matching the existing dashboard `{text, severity, source_call_ids}` shape. Rubric rebalanced: `ai_call_signal 0.50 + call_cadence 0.20 + overdue_action_items 0.10 + latest_nps 0.20`. `open_action_items` retired (double-counting with overdue + the AI signal's qualitative action-item read). `concerns.py` + `GREGORY_CONCERNS_ENABLED` gate retired — concerns flow directly from the AI signal. Never-called-clients-land-green (M3.4 known issue) fully resolved by the rebalance: never-called clients now land at score=55 yellow. SweepResult carries `duration_ms + avg_per_client_ms` for the cron-ceiling watchpoint.
- **Default-collapse Financials + Profile sections on detail page (2026-05-08).** `components/client-detail/financials-section.tsx` and `components/client-detail/profile-section.tsx` now pass `defaultOpen={false}` to the shared `Section` primitive. Other 5 sections (Identity, Lifecycle, Activity, Adoption, Notes) stay default-expanded. Always-collapse-on-load (no localStorage / per-user state) — Financials + Profile are under-used and cluttered the page; CSMs click to expand when needed.
- **Journey stage taxonomy pinned (2026-05-08, migration 0028).** Six-value funnel CHECK constraint on `clients.journey_stage`: `business_setup` / `business_setup_activation_done` / `prospecting` / `first_closing_call_taken` / `first_closed_deal` / `ten_k_month` (or null). Replaced the V1 free-text shape that 0017 + 0018 explicitly anticipated formalizing later. Pre-apply data was 100% NULL across all 192 active clients — zero backfill needed; the cheapest possible window to add the constraint. Detail-page edit field switched from free-text to enum dropdown via `JOURNEY_STAGE_OPTIONS` in `lib/client-vocab.ts`. `JourneyStagePill` becomes label-aware. `agents/ella/prompts.py` fallback updated `"active"` → `"unknown"` since "active" is no longer in the taxonomy. CHECK applies to `clients.journey_stage` only, NOT to `client_journey_stage_history.journey_stage` — mirrors the 0019 status pattern; history is append-only audit.
- **Clients list V2 columns (2026-05-08).** 1-to-1 column swap on `app/(authenticated)/clients/clients-table.tsx`: out — Last call / Open action items / Tags. In — NPS standing / Trustpilot / Meetings this month. New `NpsStandingPill` + `TrustpilotPill` components with palettes matching the existing pill family (emerald/amber/rose/sky). Vocab labels in `lib/client-vocab.ts` updated so filter dropdown labels exactly match table pill labels: NPS_STANDING_OPTIONS now uses "Promoter" (was "Strong / Promoter" — Airtable-form artifact dropped); TRUSTPILOT_OPTIONS now uses "Given"/"Declined" (was "Yes"/"No"). Underlying enum values unchanged. Default sort flipped from `last_call_date desc` to `latest_health_score asc` (worst first) — surfaces attention-needing clients at the top now that V2 brain produces reliable scores; NULL-score clients sink to bottom via NULLS-LAST.
- **Clients list editable cells + reorder + back-nav (2026-05-11).** Scott-driven slice (`docs/specs/gregory-list-editable-reorder-refresh.md`). Column order now: Name / Status / Journey stage / Primary CSM / **CSM Standing** (new column) / NPS standing / Trustpilot / Health score / Meetings this mo. Four cells inline-editable via `app/(authenticated)/clients/editable-cell.tsx` (new file, four wrapper Client Components): status / journey_stage / csm_standing route through the three dedicated history-writing Server Actions (`updateClientStatusAction`, `updateClientJourneyStageAction`, `updateClientCsmStandingAction`); trustpilot_status routes through the generic `updateClientField`. Each cell calls `router.refresh()` inside `startTransition` after a successful save so the row re-reads any cascade side effects (e.g., editing status to a negative value fires the M5.6 cascade trigger — same as the detail page). `/clients/page.tsx` gains `export const dynamic = 'force-dynamic'` so return-from-detail navigations re-fetch fresh DB rows instead of serving the static-route cache. Back-nav on `[id]/page.tsx` replaced with `BackToClientsButton` Client Component (`app/(authenticated)/clients/[id]/back-to-clients-button.tsx`) — `router.back()` when `window.history.length > 1` (preserves filter/sort URL params from the previous list view), Link href `/clients` fallback for cold-start direct entry (Slack share, deep link). Editable cells are NOT wrapped in `<Link>` (click-to-edit vs click-to-navigate are mutually exclusive); the row's `cursor-pointer` was removed since not all cells navigate. V1 visual rough edges: empty `<Label>` inside each editable cell (EditableField always renders one), no per-cell width tightening — Drake accepted V1 ugly per spec.
- **`latest_nps` signal source fix (2026-05-08).** `agents/gregory/signals.py:compute_latest_nps` now reads from `clients.nps_standing` (Airtable mirror via Path 1 + the 0027 NPS-is-gospel auto-derive) instead of `nps_submissions.score` (which stayed empty in production through M5). Mapping: promoter→100, neutral→50 (with distinct "(passive)" note), at_risk→0, NULL→neutral 50 with "no record" note. Defensive unexpected-value fallback past the 0021 CHECK. Score-shift on next sweep: at-risk clients drop ~10 points, promoter clients rise ~10 points — the rubric becomes appropriately discriminating; not a regression.
- **NPS-is-gospel auto-derive (2026-05-08, migration 0027).** Override-sticky semantics on `update_client_from_nps_segment` (M5.4 / 0021) flipped to "NPS is gospel" — every NPS Survey segment write now unconditionally auto-derives `csm_standing` via the existing `update_client_csm_standing_with_history` delegation. Override-sticky branches removed; `'problem'` stays manual-only because no segment maps to it. One-time backfill in the migration realigned 16 stale rows (14 master-sheet seeds + 2 stale Gregory Bot auto-derives, zero current CSM manual overrides per dry-run 2026-05-08). Receiver's `auto_derive_applied` flag is now always `true` on the 200 path; preserved for response-shape stability. NPS harness refactored to self-seeded fixture pattern (per-run RUN_TOKEN-suffixed unique email, hard-deleted on teardown) replacing the static-Branden fixture that broke when Branden was archived in M5 cleanup; 10 paths total (2 happy + 2 NPS-is-gospel + 6 negative).
- **Gregory brain V2 daily-cron + freshness filter (2026-05-08).** Cron schedule switched weekly→daily and `compute_ai_call_signal` gained a freshness check: before any LLM work, query `agent_runs` for the last successful compute timestamp for this client + max `documents.created_at` for `call_review` docs; if no new reviews since last compute, return the prior Signal verbatim (note rewritten to surface skip provenance + preserve original LLM-judged reasoning) and skip the Sonnet call. Skip-path opens an `agent_runs` row with `output_summary` starting with `"skipped"` so cost rollups split skip-rate from compute-rate. Each daily sweep now fires Sonnet for ~10 clients (typical new-review velocity) instead of all 188, fitting the 300s `maxDuration` ceiling. Trigger_type renamed `weekly_brain` → `scheduled_brain`. Defensive fallback: V1.1→V2 transition rows (no `ai_call_signal` entry in factors.signals[]) trigger recompute rather than returning malformed prior data. Freshness applies ONLY to `ai_call_signal`; deterministic signals (cadence, overdue, NPS) always recompute every sweep. Accepted 24h-max race: a Fathom auto-review landing AFTER the brain has read freshness for that client mid-sweep goes invisible until the next daily sweep — explicit trade-off of the architecture, not a bug.
- **Ella V2 Batch 1 — cloud Slack ingestion (2026-05-09).** Realtime ingestion of every `message`-type event from every client-mapped Slack channel, plus a one-shot historical backfill, both writing to `slack_messages`. Modules: `ingestion/slack/realtime_ingest.py` (new) is wired into `api/slack_events.py`'s `event_callback` dispatcher with a parallel branch alongside the existing (preserved-verbatim) `app_mention` handler. `shared/slack_identity.py` (new) resolves Ella's user_id behind `SLACK_USER_TOKEN` via `auth.test` so her own posts tag as the new `author_type='ella'`. Author-type vocab now: `client / team_member / ella / bot / workflow / unknown` (no migration — `slack_messages.author_type` has no CHECK; verified 2026-05-09). Parser now also includes `message_deleted` in `_SYSTEM_SUBTYPES` (preserve audit trail rather than overwrite). Audit ledger via `webhook_deliveries.source='slack_message_ingest'` with dual-discriminator pattern matching `cs_call_summary_post.py` precedent (CHECK on `processing_status` only allows `received/processed/failed/duplicate/malformed` — skip rows use `'processed'` + `processing_error='skipped_*'` + `payload.skip_reason=*`). Operational scripts: `scripts/backfill_slack_client_channels.py` (smoke + apply for the historical pull, hard-stops on `bot_not_in_channel` and on >250 channels), `scripts/invite_ella_and_bot_to_client_channels.py` (dry-run + apply for inviting Ella + bot to every client channel). Local backfill (`ingestion/slack/pipeline.py`) now also threads `ella_user_id`. Operational runbook: `docs/runbooks/slack_message_ingest.md`. Slack app config (event subscriptions + scopes) is Drake's gate (d). **Operational rollout (2026-05-10):** backfill of 8 known-good channels completed — 3,641 rows in `slack_messages` (Musa Elmaghrabi, Javi Pena production + #ella-test-drakeonly, Trevor Heck, Dhamen Hothi, Jenny Burnett, Art Nuno, Fernando G; per-channel counts in `docs/reports/ella-v2-batch-1-finish-rollout.md`). Backfill script's `bot_not_in_channel` hard-stop softened to log-and-continue + per-error-type summary so a future full-fleet bulk run won't abort on the first non-member channel. Live ingestion verified operational 2026-05-10 after `message.groups` event subscription was added to the Slack app config — client channels are typically private (🔒), and `message.channels` alone fires only for public channels; both subscriptions are now active alongside the `channels:history` + `groups:history` scopes. The 129 remaining client channels are pending Drake-led ops work to invite Ella's bot — once added, both backfill and realtime ingestion light up automatically with no further code changes. Also surfaced (tracked in known-issues): `--channel-id` on the backfill script doesn't strictly scope to that channel id when the underlying client maps to multiple channels — workaround is the `extra_channel_names` path in `run_ingest`.
- **Ella V2 Batch 1.5 — behavioral fixes (2026-05-10).** Seven bundled changes informed by the V1 interaction audit (`docs/reports/ella-interaction-audit.md`). (1) `agents/ella/identity.py` (new) resolves real speaker `slack_user_id` → `client` / `advisor` / `unresolvable`. `slack_handler.py` stops impersonating the channel-mapped client; `agent.py` resolves channel-client and speaker separately. `agent_runs.trigger_metadata` now carries `real_author_role/name/id` for honest analytics. (2) `prompts.py:_render_speaker_section` renders audience-aware persona block — V1 client behavior stays for clients, advisors get an explicit "Do NOT escalate, Do NOT emit [ESCALATE]" persona, unresolvable speakers get a safer-fallback persona. (3) On escalation Ella now writes an explicit `<@advisor_slack_user_id>` Slack mention in the client-facing ack so the advisor gets notified in real time; advisor mention syntax comes from the WHO IS SPEAKING prompt section. (4) `[ESCALATE]` detector flipped from "match at start only" to "match anywhere"; everything before the marker → client message, everything after → `escalations.context.handoff_reasoning` for the reviewing CSM. Catches both audit-flagged mid-response leaks (runs c84d63e1 and da7a4ee1). (5) `_post_to_slack` no longer threads — main-channel-only responses, with conversational context coming from a new `agents.ella.retrieval.fetch_recent_channel_context` (last 15 messages in the channel before the trigger, oldest-first, capped ~8000 chars). Subsumes future-ideas V2.1 + V2.2. (6) Bare @-mentions (stripped text <5 chars) skip Claude and return a randomized warm opener via `_handle_bare_mention`; logged as `agent_runs` with `trigger_type='bare_mention'` and zero token cost. Fixes audit run 88556dea. (7) Dual-trigger detection in `api/slack_events.py:_should_dual_trigger` — `message` events that @-mention Ella's human user_id (and not the bot) get reshaped to `app_mention` shape and dispatched through `_process_mention`. Doc updates: `docs/agents/ella/ella.md` (Trigger, Response Location, escalation flow, Style examples, System Prompt Direction point 10), `docs/agents/ella/future-ideas.md` (V2.1/V2.2 superseded; V2.3/V2.4 completed). Test suite: +28 new Ella-related tests (`test_identity.py`, `test_prompts.py`, `test_retrieval_recent_context.py`, `test_slack_events_dual_trigger.py`, plus additions to `test_agent.py` for bare-mention / detector / speaker plumbing). Drake validates in `#ella-test-drakeonly` post-deploy (gate c).
- **Ella V2 Batch 2.2 — audit dashboard (2026-05-10).** New Gregory dashboard page at `/ella/runs` (list) + `/ella/runs/[id]` (detail). Read-only surface scoped to `agent_runs WHERE agent_name='ella'`. List view has a summary band (today / week / month run counts, status mix over 30 days, cost, anomaly count), a filter bar (date range, channel multi-select, speaker role, status, anomaly flag, "Show anomalies only" toggle — all URL-state-serialized), and a paginated table (50/page). Detail view shows run header (real author + role, status, anomaly flags, cost · tokens), input, surrounding thread context (resolved display names, triggering message highlighted), Ella's response (split into client-facing vs captured `handoff_reasoning` when `[ESCALATE]` was detected), escalation row when present, full `trigger_metadata` JSON. Five anomaly checks mirror `scripts/audit_ella_interactions.py`: A (ESCALATE leak), B' (real-author mismatch — uses Batch-1.5 `trigger_metadata.real_author_id` field, pre-1.5 runs render as `role=unknown`), C (error), D (length outlier ±5%), E (bare mention). Query layer: `lib/db/ella-runs.ts`. UI: `app/(authenticated)/ella/runs/`. Top-nav gains "Ella" link. Prereq landing before Batch 2.3 passive monitoring so post-passive run volume is operationally manageable. Drake validates in production via gate (c).
- **Ella V2 Batch 2.3 — passive monitoring (2026-05-11).** Passive trigger pipeline in `ingestion/slack/realtime_ingest.py` forks to `agents/ella/passive_monitor.py:evaluate_passive_trigger` for every client message in a `slack_channels.passive_monitoring_enabled=true` channel. Pre-Haiku gates (global kill switch via `ELLA_PASSIVE_MONITORING_ENABLED`, author-type, CSM-directed via mention or first-name match, KB-relevance via `shared.kb_query.search_for_client` against the channel-mapped client's scope, firm-after-first via keyword-overlap against recent escalations) cheap-skip; Haiku (`claude-haiku-4-5-20251001`) decides one of `respond_substantive` / `respond_general_inquiry` / `skip` / `escalate`. Respond decisions queue to the new `pending_ella_responses` table with a 4-minute delay via `agents/ella/passive_dispatch.py:persist_passive_evaluation`; the new per-minute Vercel cron at `/api/passive_ella_cron` drains the queue, re-checks the kill switches + per-channel toggle + CSM-intervention (via `slack_messages` table read — covers main + thread automatically), and on intervention-free rows dispatches to `agents/ella/agent.py:respond_to_passive_trigger` (substantive — full Sonnet generation reusing the reactive prompt path) or `:handle_passive_general_inquiry` (canned warm opener, zero LLM cost). Escalations are backend DMs to the channel's primary_csm via `shared/slack_post.post_message` with a Slack deep-link to the triggering message + truncated `haiku_reasoning` — no quoted client content; audited under `webhook_deliveries.source='ella_passive_escalation_dm'`. Default-stance is **stay out**: every uncertain case skips silently. Migrations 0029 (rename `slack_channels.ella_enabled` → `passive_monitoring_enabled` + atomic CREATE OR REPLACE of the onboarding RPC to use the new column name in its Branch C INSERT) + 0030 (create `pending_ella_responses` queue table). Dual kill switches — env var `ELLA_PASSIVE_MONITORING_ENABLED` + per-channel `slack_channels.passive_monitoring_enabled` boolean — both default OFF at ship; Drake enables `#ella-test-drakeonly` first for validation per `docs/runbooks/ella_passive_monitoring.md`. **Follow-up 2026-05-11:** migration 0031 added `slack_channels.test_mode boolean default false` which, when true, widens Gate 2 from clients-only to `client`+`team_member` (still rejects ella/bot/workflow/unknown) so Drake can smoke-test as himself in `#ella-test-drakeonly`. Test-mode runs are tagged in `agent_runs.trigger_metadata.test_mode_run=true` for audit-filter purposes. Same follow-up extended `lib/db/ella-runs.ts` with shape-agnostic `extractChannelId` / `extractAuthorRole` / `extractAuthorName` adapters that read both the reactive-path (`channel`, `real_author_*`) and passive-path (`triggering_slack_channel_id`, `author_type`, `triggering_message_slack_user_id`) `trigger_metadata` shapes — fixes the "unknown author / unknown channel" rendering bug from the 2026-05-11 post-rollout investigation report. Firm-after-first instruction also added to the Sonnet system prompt (`agents/ella/prompts.py`) — affects both reactive @-mention substantive responses and passive substantive responses since they converge on the same `build_system_prompt` output. Test suite: +40 tests across `tests/agents/ella/test_passive_monitor.py` (18, six gates + Haiku parse), `tests/agents/ella/test_passive_dispatch.py` (6, four decision outcomes + cost accounting), `tests/api/test_passive_ella_cron.py` (13, per-row gates + auth), `tests/ingestion/slack/test_realtime_ingest_passive_fork.py` (3, fork dispatch). Total suite passing: 507 tests post-Batch-2.3 (run via `.venv/bin/python -m pytest tests/`). Drake validates in production via gate (c) after flipping `ELLA_PASSIVE_MONITORING_ENABLED=true` + the per-channel toggle on `#ella-test-drakeonly`. **Rollout 2026-05-11:** post-validation, `passive_monitoring_enabled=true` flipped on the 7 production client channels from the Batch 1 backfill cohort (Art Nuno, Dhamen Hothi, Fernando G, Javi Pena, Jenny Burnett, Musa Elmaghrabi, Trevor Heck — `test_mode=false` on all 7; `#ella-test-drakeonly` retains `test_mode=true` for ongoing smoke-testing). Per the resolution SELECT, "Ashan Fernando" matched the `%fernando%` pattern but was excluded — not in the rollout cohort.
- **Test suite:** 414 passing (up from 381 baseline at start-of-day 2026-05-07; +33 across `tests/agents/call_reviewer/`, `tests/shared/ingestion/test_validate.py`, `tests/agents/gregory/test_ai_call_signal.py`, plus updates to `test_signals.py` / `test_scoring.py` / `test_agent.py` / `tests/ingestion/fathom/test_pipeline.py`). After V2 Batch 1: +13 ingest tests in `tests/api/test_slack_events_message_ingest.py`, +9 in `tests/shared/test_slack_identity.py`, +5 parser additions in `tests/ingestion/slack/test_parser.py`. Post-2026-05-11 director-docs-topology-and-root-cleanup spec: 512 passing.

---

## Gregory editorial skin shipped

### 2026-05-19 — Ella decision-Haiku prompt sharpening

`docs/specs/ella-decision-haiku-prompt-sharpening.md`. Surgical fix for a production over-skip regression from the unified-path refactor: the decision Haiku was skipping @-mentions because the speaker resolved as `advisor` (matching default-skip-advisor) and a 22-hour-old escalation was still in the context window (read as "the active conversation"). Prompt-only + one rendering change — no architecture, no tables, no crons, no migrations, no env vars.

**`agents/ella/passive_monitor.py`:** `_HAIKU_SYSTEM_PROMPT` rewritten **verbatim from the spec** (hard stop #4 — verified byte-equal). Four changes: (1) the @-mention rule promoted to a `# THE @-MENTION OVERRIDE (READ THIS FIRST)` section placed *before* `# THE THREE DECISIONS`; (2) hardened from "strongly lean" to an **absolute structural override** — `is_ella_mentioned: true` ⇒ decision MUST be `respond`/`acknowledge_and_escalate`, `skip` FORBIDDEN unless the @-mention is referential, advisor speakers explicitly do not bypass; (3) bare-mention threading made non-negotiable (answer the most recent prior question); (4) new `# READING TIME-STAMPED CONTEXT` section with explicit decay bands — 0-4h ACTIVE / 4-24h RECENT-but-fresh / 24h+ STALE / 7d+ IGNORE, with "do not skip a current @-mention because of a stale prior escalation." `skip` now explicitly gated on `is_ella_mentioned: false`.

**`agents/ella/retrieval.py`:** new `_format_time_ago(seconds)` helper; `fetch_recent_channel_context` line format gains a pre-computed delta → `[YYYY-MM-DD HH:MM ET — <delta>] <role> (<name>): <text>`, with a new `relative_to: datetime | None` param (defaults to `now(UTC)`). `passive_monitor._evaluate` passes `relative_to` derived from the Slack trigger ts (which *is* the message's unix timestamp — no `slack_messages` lookup needed; spec assumed a lookup, simpler+always-available judgment call). `fetch_recent_channel_messages` unchanged.

Post-state: **41 migrations, 13 Python serverless functions, 635 pytest passing** (baseline 626 — +9 net for the new prompt-structure / time-ago / behavioral tests). `ruff check` clean on all touched files; `ruff format` applied; `tsc --noEmit` + `next lint` clean (no TS touched). Docs: `ella.md` (System Prompt Direction point 12 + new Recent Context Format subsection + changelog), this entry. **Drake gates: (a) none; (d) none; (c) 5-case smoke in `#ella-test-drakeonly` post-deploy** — until smoke passes the spec stays `in-flight` and the report is `(PARTIAL)`. One pre-existing out-of-scope `ruff` item (`unused import pytest` in `tests/agents/ella/test_agent.py`, untouched by this spec) is left as-is per the documented prior-baseline precedent.

### 2026-05-18 — PM: Ella unified-path intelligence refactor

`docs/specs/ella-unified-path-intelligence-refactor.md`. Corrective follow-up to the morning's spec — Drake smoke-tested in `#ella-test-drakeonly`, found three real problems (double-fire on @-mention, Haiku answering "where do I find X" as if it were "what is X", conversation context not load-bearing), and Director wrote this.

**One path.** Reactive (@-mention) and passive collapsed into a single pipeline. `api/slack_events.py` no longer dispatches `app_mention` to a reactive handler — it's a logged no-op; Slack's parallel `message` event flows through realtime ingest → `passive_monitor` → `passive_dispatch` exactly once (kills the double-fire). `_should_dual_trigger` / `_build_app_mention_from_message` / `_process_mention` removed. `agent.respond_to_mention` is now a thin adapter over that same one path (kept so legacy callers / `slack_handler` still resolve); bare-mention short-circuit + `handle_passive_general_inquiry` + the canned-opener constants removed. `respond_to_passive_trigger` (the per-minute cron's Sonnet drain) stays.

**Two gates, three decisions.** `passive_monitor` keeps only kill-switch + author-type (client + team_member always evaluated — the test_mode carve-out is now the default; ella/bot/etc skip with an audit row). The decision Haiku returns `respond` (+ `response_model: haiku|sonnet`) / `acknowledge_and_escalate` (+ Haiku-written `ack_text`) / `skip`, plus the independent digest flag. `acknowledge_and_escalate` ALWAYS posts a warm in-channel ack (client never sees silence on a human-needed message) AND fires the Scott+advisor DM AND writes the digest item — on every message, @-mention or not. The `[FALLBACK_TO_SONNET]` mechanism is fully removed (decision-layer model-pick replaces it; `DigestResponseResult.fallback_to_sonnet` is vestigial-False for one release). CSM-directed / KB-relevance / firm-after-first are now soft rules in the (verbatim-from-spec) decision prompt, not hardcoded gates.

**Retrieval upgrades.** KB search query is now built from the combined recent conversation (last 6 messages incl. Ella's own + triggering message ×2) via new `retrieval.build_kb_query_from_conversation`, not just the triggering text. `fetch_recent_channel_context` line format changed to `[YYYY-MM-DD HH:MM ET] <role> (<name>): <text>` (team_member→advisor, ET via zoneinfo, Ella's posts included) so Haiku can weigh recency for active-conversation detection. New `fetch_recent_channel_messages` row primitive. `is_ella_mentioned` detected in realtime_ingest (bot OR human uid) and threaded through `PassiveTriggerPayload` as the strongest decision signal.

No migrations, no env-var changes, no new crons. Migration 0040 + the daily digest cron from the morning stay as-is. Post-state: **40 migrations, 12 Python serverless functions, 610 pytest passing** (baseline 609; the passive_monitor/dispatch/agent/retrieval/slack-events test files were rewritten for the collapsed contract). `tsc --noEmit` + `next lint` clean. Docs: `ella.md`, `ella_passive_monitoring.md`, this entry. **Drake gates: (a) none — no migrations; (c) post-deploy smoke in `#ella-test-drakeonly` across the spec's 8 cases incl. the double-fire check, + manual digest curl.** (Caveat carried forward: the morning spec's report stays `(PARTIAL)` — its gate-(c) smoke produced *this* spec's findings rather than a clean pass; line-11 migration-inventory paragraph remains stale, flagged in the morning entry, untouched here.)

### 2026-05-18 — Ella architecture refactor + daily digest

`docs/specs/ella-architecture-refactor-and-daily-digest.md`. Two architecturally-inseparable changes shipped together.

**(1) Passive-monitor unified-decision rewrite.** `agents/ella/passive_monitor.py` collapsed from 5 pre-LLM gates to **2** (global kill switch → author-type). The CSM-directed, KB-relevance (+`_ESCALATION_BYPASS_KEYWORDS`), and firm-after-first gates are gone — Haiku decides all of it now (Drake's call: let Haiku decide more, let pre-LLM gates do less). KB vector search stays but is context, not a gate. New `PassiveDecision` contract: `decision ∈ {skip, respond_haiku_self, respond_via_sonnet, digest_only}` + independent `digest_flag: bool` + `digest_category`. `passive_dispatch.py` rewritten: kill-switch skip writes **no** `agent_runs` row at all; `respond_haiku_self` runs a new response Haiku (`agents/ella/digest_response.py`) that posts directly or emits `[FALLBACK_TO_SONNET]` to route to the Sonnet queue (written as `respond_substantive` so the unchanged per-minute cron drains it); `respond_via_sonnet` queues Sonnet; `digest_only` writes a `pending_digest_items` row only — **no `escalations` row, no CSM DM on the passive path anymore** (`_write_passive_escalations_row` deleted). The reactive @-mention path (`agent.py`) now runs through the same decision Haiku; `[ESCALATE]` token + `_detect_and_strip_escalation` + Sonnet self-escalation all removed (single-decider). Reactive `digest_only` is the **only** real-time CSM-DM path (polite ack + `escalations` row + `fire_escalation_dms` + digest item) — the deliberate @-mention-vs-passive asymmetry. `prompts.py` `_BASE_PROMPT` lost WHAT YOU ESCALATE + FIRM AFTER FIRST, gained a short fallback section; `_ESCALATE_LITERAL_FOR_PROMPT` → `_FALLBACK_LITERAL_FOR_PROMPT`.

**(2) Daily digest.** New `pending_digest_items` table (migration **0040**, public table count → per the running count; the table is insert-once + a single `sent_in_digest_at` UPDATE, dedup unique index on `(slack_channel_id, triggering_message_ts)`, partial unsent index). New cron `api/ella_daily_digest_cron.py` (`/api/ella_daily_digest_cron`, `30 20 * * *` UTC = 16:30 EDT / 15:30 EST) drains unsent rows in a trailing 24h window (or `?since=` override), groups by client, DMs Scott (resolved via `team_members.access_tier='head_csm'`) + optional CC `ELLA_DAILY_DIGEST_CC_SLACK_USER_ID` (Drake). Empty days still fire. Per-recipient `webhook_deliveries.source='ella_daily_digest'` audit. Mirrors the FAQ-digest pattern.

Post-state: **40 migrations, 12 Python serverless functions, 6 TopNav tabs, 609 pytest passing** (baseline 607 — net +2 after the passive-monitor / passive-dispatch / agent test files were rewritten for the collapsed architecture, consolidating many now-obsolete per-gate tests into focused coverage of the new decision tree). `tsc --noEmit` + `next lint` clean. Docs: `ella.md`, `ella_passive_monitoring.md`, `cron_schedule.md`, new `ella_daily_digest.md`, new `pending_digest_items.md`, `.env.example`. **Drake gates open: (a) migration 0040 SQL review + apply [held at this gate]; (c) post-deploy smoke in `#ella-test-drakeonly` for all 4 decision branches + manual digest curl; (d) `ELLA_DAILY_DIGEST_CC_SLACK_USER_ID=U0AMC23G1SM` in Vercel Production.** Note: `docs/state.md`'s migration-inventory paragraph (line 11) lags reality — it still says "36 migrations applied (0001–0036)" while the migrations dir + ledger are at 0040; flagged for an EOD reconciliation, not corrected here (out of this spec's scope).

### 2026-05-15 — EOD (evening): Gregory V1 closed — cost hub + FAQ harvest + title v2 + timezone alignment

Seven specs landed after the morning EOD. Gregory V1 is now closed: the admin-tier cost hub at `/cost-hub` (migration 0038) gives Nabeel five Anthropic LLM-spend buckets + editable monthly subscriptions / one-off extras + a 12-month history view, with subscription `effective_from` month-attribution (migration 0039) so a sub added today no longer inflates prior months. The call_reviewer prompt went to v2 (`questions_asked` extraction) feeding a new Friday Slack DM to Scott (`api/faq_digest_cron.py`, optional `FAQ_DIGEST_CC_SLACK_USER_ID` CC), and the May 2026 backlog was regenerated under v2. Ella's passive-monitor escalation thresholds were lowered (uncertainty / confusion / clarification-seeking now surface through Gate 4), the Trustpilot cascade gained a first-month carve-out (migration 0037), and a scenario-1 audit fixed the previously-untracked sentiment-classifier Haiku spend. Title convention v2 (`[Client Name] - Coaching/Sales Call with {Scott|Lou|Nico}`) extends the May 18 forcing function with name-prefix-as-primary client resolution. A cost-hub-vs-`/ella/runs` discrepancy was diagnosed (different windows + scope, not a timezone bug) and resolved by aligning `getEllaSummaryStats` to EST calendar boundaries + all-LLM-cost-bearing scope via a shared `lib/time/est-periods.ts`; the store-UTC/render-ET standard is codified in ADR 0003 with a `docs/runbooks/cron_schedule.md` UTC→ET map. Naming polish: "Cost hub" → "Cost Hub" in the page + nav.

Post-state: **39 migrations, 11 Python serverless functions, 6 TopNav tabs (Clients / Calls / Teams / Ella / Cost Hub / Tasks), 607 pytest passing**, `tsc --noEmit` + `next lint` clean. ADRs: 0001 (foundational stack), 0002 (title-convention enforcement + v2 revision), 0003 (timezone conventions). Per-spec evening-cycle detail in the entries below. Drake gates still open: (d) `FAQ_DIGEST_CC_SLACK_USER_ID` in Vercel + FAQ digest first fire (Fri May 22); (c) eyeball `/cost-hub` History after a real backdated sub, and `/cost-hub` ↔ `/ella/runs` agreement on real data.

### 2026-05-15 — EOD: Director-tier surfaces + permissions infrastructure + May 18 forcing function

Ten specs landed across 2026-05-14 + 2026-05-15. Plain-English post-state:

- **Permissions infrastructure (`permissions-access-tiers`, migration 0032).** Four-tier hierarchical access on `team_members.access_tier`: `csm` < `head_csm` < `admin` < `creator`. Backfill seeded the 6 known users (Drake creator, Nabeel admin, Scott Wilson head_csm, Lou / Nico / Zain csm). Server-side gating via `lib/auth/access-tier.ts` (+ Client-Component-safe shared types). Sub-layouts gate `/ella/runs` (Admin+), `/teams` (Head CSM+), `/tasks` (Creator). TopNav filters by `requiredTier`.

- **`/teams` Meeting Tracker (`teams-meeting-tracker`, migrations 0033 + 0034).** Head-CSM-and-up dashboard. 0033 adds `oauth_tokens` (per-team-member OAuth, V1 keyed to Drake's row), 0034 adds `calendar_events` (cached calendar data). 30-min cron `api/teams_calendar_sync_cron.py` syncs via Drake's stored Google OAuth token; CSMs share their calendars with Drake at the Workspace level. Title+time JS-side join against `calls`; lateness pill when Fathom started >2 min after the calendar event. Three new env vars: `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET`, `NEXT_PUBLIC_APP_URL`.

- **`/teams` external-attendee filter + personal-email exclusion (two specs, no migrations).** Calendar events with zero non-`@theaipartner.io` attendees get dropped at upsert — OOO blocks, focus time, internal-only 1:1s no longer clutter `/teams`. `team_members.metadata.personal_emails` adds teammates' non-AIP addresses to the internal list (Huzaifa's gmail seeded today) so internal-only meetings don't leak past the filter. Two one-time cleanup DELETEs against cloud `calendar_events` removed 320 stale rows total.

- **Nabeel removed from CSM roster (migration 0035).** `team_members.is_csm` flipped false. Removes him from `/teams`, the Primary CSM dropdown on `/clients`, and the Primary CSM swap flow on `/clients/[id]`. His 3 active `client_team_assignments` rows stay intact per Drake's direction — affected clients still surface him as their assigned CSM until manually reassigned.

- **May 18 title-convention enforcement (`classifier-enforce-new-title-convention`, ADR 0002).** Hard cutoff at `started_at >= 2026-05-18T00:00:00 America/New_York`. Post-cutoff calls only auto-classify as `client` when titles match `Coaching/Sales Call with {Scott|Lou|Nico}`. Forcing function for the booking-link adoption per Nabeel's expectation. Pre-cutoff calls untouched; no retroactive reclassification. ADR 0002 captures the rationale (management lever, not a memo).

- **Auto-created client lifecycle (`auto-created-client-lifecycle`).** Closes the gap the cutoff spec left: post-cutoff client calls with unresolved external participants now emit `AutoCreateRequest` → pipeline reifies a `clients` row tagged `needs_review`. UI side: Merge button re-surfaced on `/clients/[id]` under the needs_review gate; new "Mark as reviewed" button beside it; missing-Slack badges on `/clients` (new column, conditional render) + detail page (always renders when applicable) + "Missing Slack" filter chip.

- **`/tasks` Director task page + list UX polish (`director-tasks-and-list-ux-polish`, migration 0036).** Creator-tier-only single-user personal task list. Add/toggle/delete server actions; open + done sections with strike-through. TopNav gains a "Tasks" entry visible only to Drake. Sticky list filters on `/clients` + `/calls` via `?from=<encoded path+query>` URL param; back buttons validate the prefix (open-redirect defense). "Clear filters" button on both filter bars preserves the search query.

- **Ella escalation unification (`ella-escalation-unify-and-route-to-scott`, 2026-05-14).** Both reactive and passive escalation paths route DMs through `agents/ella/escalation_routing.py` to Scott (`ESCALATION_RECIPIENT_SLACK_USER_ID`) + the channel's primary CSM. Passive escalations now also write an `escalations` row (was: only audit ledger). Prompt edit dropped `<@advisor>` mention from the in-channel ack since the backend DMs handle notification.

- **Ella passive Gate 4 escalation-keyword bypass (`ella-passive-escalation-keyword-bypass`, 2026-05-14).** Closes the gap where escalation-worthy messages with no curriculum anchor (KB similarity <0.30) died at Gate 4 before Haiku saw them. `_ESCALATION_BYPASS_KEYWORDS` frozenset covers five categories (money, complaints, crisis, quitting, legal). Bypass routes to Haiku — Haiku still decides.

- **Gregory wrap day (`gregory-day-end-wrap-2026-05-14`).** Six workstreams from the prior session — Primary CSM editable on `/clients[/[id]]`, NPS / Accountability toggle pills, action items transfer fix, Send-to-Slack action items (dry-run-validated, production cutover pending), Ella pre-redesign fixes + visual redesign by Claude Design + Builder via the design-handoff workflow.

Post-state: **36 migrations, 10 Python serverless functions, 5 TopNav tabs (Clients / Calls / Teams / Ella / Tasks), 575 pytest passing**, `tsc --noEmit` + `npm run lint` clean. ADRs: 0001 (foundational stack), 0002 (title-convention enforcement). Per-spec detail below stays for chronological reference.

---

- **Ella threshold lowering + Trustpilot first-month carve-out + FAQ harvest shipped (2026-05-15, `docs/specs/ella-threshold-trustpilot-first-month-faq-harvest.md`).** Three Scott-driven asks bundled into one ship. **(1) Ella passive escalation-bypass keyword extension.** `_ESCALATION_BYPASS_KEYWORDS` in `agents/ella/passive_monitor.py` gained four new categories — uncertainty / mismatched expectations / clarification-seeking / soft frustration (22 new keywords). Scott explicitly fine with FPs because Haiku still arbitrates; sanity-checked against 1,568 historical client `slack_messages` (max match rate 0.26% on `"lost"`, well under the spec's 5% hard threshold). **(2) Trustpilot first-month carve-out, migration 0037.** `clients_trustpilot_cascade_on_happy_before` trigger WHEN clause extended with two new gates: `NEW.start_date IS NOT NULL` and `NEW.start_date <= current_date - interval '30 days'`. Postgres can't mutate trigger WHEN via CREATE OR REPLACE, so the migration drops + recreates. Forward-only — existing happy clients with non-`'ask'` trustpilot stay where they are. Per Scott's "don't ask new clients for reviews" intent. Applied + dual-verified post-apply (`pg_get_triggerdef` shows the new 4-clause WHEN; `schema_migrations.version='0037'` registered; trigger function body unchanged). **(3) FAQ harvest pipeline.** Call reviewer prompt bumped to v2 (`agents/call_reviewer/prompt.py`) with a new `questions_asked` fifth top-level JSON key — captures every FAQ-useful question raised during the call, tagged with `asker='client'|'csm'` so downstream consumers filter without re-running the LLM. `_REQUIRED_KEYS` + `_validate_review_shape` in `reviewer.py` extended. May 2026 backfill regen ran on all 82 existing reviews under the v2 prompt (delete-then-re-apply per the script's documented pattern — the script filters out already-reviewed calls upstream, so in-place upsert wasn't an option). New weekly cron at `api/faq_digest_cron.py` fires Friday 19:00 UTC (15:00 EDT during DST, 14:00 EST after fallback) — pulls last 7 days of call_review documents, extracts every client-asked question, cluster-light dedups via token-Jaccard (threshold 0.5, no LLM), sorts by call count, caps at top 50 clusters, DMs Scott via `shared.slack_post.post_message` against his dynamically-resolved `team_members.slack_user_id`. `vercel.json` gained the function entry + cron schedule; `webhook_deliveries.source='faq_digest_cron'` audits each fire. Test suite: 31 passing on `tests/agents/ella/test_passive_monitor.py` (Task 1 — no new tests needed; existing parametrized "each category triggers" coverage holds), 22 passing on `tests/agents/call_reviewer/test_reviewer.py` (+2 for missing-questions_asked + wrong-type-for-questions_asked), 14 passing on new `tests/api/test_faq_digest_cron.py` (happy path, zero-questions, csm-asked filter, malformed entries, non-JSON content, cluster dedup behavior, cluster count surfacing, missing-Scott failure, 4 auth cases, Slack-failure isolation). Post-state: **37 migrations, 11 Python serverless functions, 5 TopNav tabs**. Drake's gates remaining: (c) post-deploy curl of `/api/faq_digest_cron` to validate the first real fire, then the first scheduled Friday tick. New runbook at `docs/runbooks/faq_digest.md` covers schedule + DST drift + audit SQL + failure modes + tuning surfaces.

- **Ella summary ↔ cost hub alignment + timezone ADR (2026-05-15, `docs/specs/ella-summary-est-alignment-and-timezone-adr.md`).** Follow-up to the cost-hub-vs-ella diagnostic. (1) New `lib/time/est-periods.ts` is the canonical EST/EDT calendar-boundary module (`getEstPeriodBoundary`, `getEstOffsetMinutes`, `getEstMonthStart`); `lib/db/cost-hub.ts` refactored to import from it (pure move, zero logic change). (2) `getEllaSummaryStats` rewritten: server-local `setHours`+rolling-7d/30d → EST calendar boundaries via the shared module, AND response-scope filter → all-LLM-cost-bearing scope (`agent_name='ella' AND llm_cost_usd IS NOT NULL`). `/ella/runs` summary now matches the cost hub's combined Ella Sonnet+Haiku buckets exactly — cross-checked against cloud post-fix: delta $0.00 / equal run counts on today ($0.0758) / week ($0.8974) / month ($1.2437). Dead `skip_cost_today` + response/skip-scope machinery removed; `errors_*` widened to all Ella errors in the EST window. (3) New ADR 0003 (store-UTC/render-ET standard, period defs, code home, no-sweep scope boundary) + new `docs/runbooks/cron_schedule.md` (all 6 vercel.json crons' UTC→ET map, strict-JSON workaround). No migration, no env changes. `tsc --noEmit` + `next lint` clean; pytest 607 (no Python touched). The `getEllaSummaryStats` server-local-time deviation is logged as fixed in ADR 0003.

- **Cost-hub effective_from + title convention v2 shipped (2026-05-15, `docs/specs/cost-hub-effective-from-and-title-convention-v2.md`).** Two independent fixes bundled. **Task 1 (migration 0039):** `monthly_subscriptions.effective_from date NOT NULL DEFAULT CURRENT_DATE`, existing rows backfilled to `created_at::date`. Fixes the cost-hub history bug where every non-archived sub was summed into every historical month — a sub added today inflated April/March/etc. New `subscriptionActiveInMonth(sub, monthStart, monthEnd)` helper in `lib/db/cost-hub.ts` (rule: `effective_from <= last_day_of_M AND (archived_at IS NULL OR archived_at >= first_day_of_M)`); `getMonthTotal` filters via it (using a new `fetchSubscriptionsForHistory` that includes archived subs, since an archived sub still counts toward its active window); page.tsx filters the current-month sub list once for both the editable table + running total. `effective_from` date input added to the add form + per-row edit; server actions gain an `effectiveFrom` param with YYYY-MM-DD validation + today-EST default. Backdating use case supported (forgot to log a sub for prior months → set effective_from back). Applied + dual-verified (column NOT NULL + default; all rows = 2026-05-15; ledger 0039). Verifier extended (backdated + today-dated subs, both visible in active table); rollup correctness SQL-proven (backdated counts in March history, today-dated doesn't). **Task 2 (no migration):** classifier accepts the v2 booking-link shape `[Client Name] - Coaching/Sales Call with {Scott|Lou|Nico}` alongside v1 — both valid indefinitely, no second cutoff. v2 uses the name prefix as the PRIMARY client-resolution signal (`ClientResolver.lookup_by_name`), participant email is the fallback; `classification_method='title_pattern_v2'` (v1 stays `'title_pattern'`); `call_type` from the regex capture. `_V2_TITLE_RE` + `_extract_v2_title_prefix_and_type` + extended `_matches_new_client_title_convention`/`_classify_by_new_convention`. 11 new classifier tests (596 → 607); existing 46 cutoff tests unchanged + green. 0 duplicate non-archived `full_name` groups (well under the spec's >5 collision threshold). ADR 0002 gained a "Revision 2026-05-15" section; `call_title_convention.md` documents the v2 pattern. `tsc --noEmit` + `next lint` clean; full suite 607 passing. Drake's gates: (a) migration 0039 SQL reviewed + applied; remaining (c) eyeball the cost-hub History view after a real backdated sub. Spec hard stops honored (gate (a) SQL review; created_at::date backfill confirmed before apply).

- **Admin cost hub shipped (2026-05-15, `docs/specs/cost-hub.md`, migration 0038).** New admin-tier-only page at `/cost-hub` — closes Gregory V1. Migration 0038 added two manually-maintained tables: `monthly_subscriptions` (provider / monthly_cost_usd numeric(10,2) / notes / soft-archive) and `cost_extras` (incurred_on date / description / cost_usd / soft-archive), both with the shared `set_updated_at()` trigger + partial index on `archived_at IS NULL`. Applied + dual-verified post-apply (both tables, both partial indexes + PKs, both triggers, ledger row 0038). Page composition: HeaderBand + total-this-month gold box (sum of Anthropic + subs + current-month extras, with a History expander showing the last 12 completed months each expandable to its bucket breakdown) + five Anthropic bucket boxes (Today / This week / This month × runs / cost / avg) + two editable tables. Data layer `lib/db/cost-hub.ts` aggregates `agent_runs.llm_cost_usd` JS-side (PostgREST has no clean `sum()`); period boundaries computed in America/New_York with DST-safe offset detection. Six admin-tier-gated server actions (add/update/delete × subs + extras), soft-archive on delete so historical totals stay accurate. Sub-layout mirrors the Ella admin gate; TopNav gains a "Cost hub" entry (admin+). **Pre-flight finding:** the spec's assumed `agent_name='gregory_brain'` doesn't exist — the Gregory brain's Sonnet cost is attributed to `agent_name='ai_call_signal'` (the `agent_name='gregory'` runs are LLM-free orchestration). Bucket 5 filters on `ai_call_signal`; UI label stays "Gregory brain Sonnet". Earliest reliable cost dates per bucket: Ella Sonnet 2026-04-24, Ella Haiku 2026-05-11, Call review Sonnet + Gregory brain Sonnet 2026-05-07; Call review Haiku never used (Sonnet-only — renders "(no usage — Sonnet-only today)"). No bucket failed the <50% completeness hard threshold. Playwright verifier `scripts/verify-cost-hub-preview.ts` passes (page render + 5 bucket boxes + total + add/delete on both tables); screenshot at `scripts/.preview/cost-hub.png`. `tsc --noEmit` + `next lint` clean; `pytest tests/ -q` unaffected (no Python touched beyond the migration). New schema docs `monthly_subscriptions.md` + `cost_extras.md`; new runbook `docs/runbooks/cost_hub.md`. Post-state: **38 migrations, 11 Python serverless functions (no new functions — cost-hub is dashboard-side), 6 TopNav tabs (Clients / Calls / Teams / Ella / Cost hub / Tasks)**. Drake's remaining gate (c): post-deploy manual data entry of real subscriptions + extras. CLAUDE.md § Next Session Priorities collapses to just "Gregory V2 — sales side."

- **FAQ digest optional CC + questions_asked surface verification (2026-05-15, `docs/specs/faq-digest-cc-and-questions-surface-check.md`).** Two-task follow-up. **(1)** New env var `FAQ_DIGEST_CC_SLACK_USER_ID` adds an optional CC recipient to the FAQ digest cron — Drake sees the same DM as Scott during validation. Send path refactored to fan out one `post_message` + one `webhook_deliveries` audit row per recipient (mirrors `ella_escalation_dm`). Malformed env var degrades to Scott-only with logged warning. **(2)** Read-only investigation confirmed `questions_asked` is cleanly isolated to `api/faq_digest_cron.py`: `lib/db/calls.ts` `CallReview` TS interface explicitly declares only the four v1 fields and `parseCallReview` validates only those; render code on `/calls/[id]/page.tsx` destructures by name. The TS type acts as a hard filter — the new field never reaches the render layer. Absence is intentional. Tests: 19 passing on `tests/api/test_faq_digest_cron.py` (14 existing + 5 new — 4 spec'd CC cases + 1 dedup edge case). No migrations. Drake's remaining gate (d): set `FAQ_DIGEST_CC_SLACK_USER_ID=U0AMC23G1SM` in Vercel Production env vars.

- **Director tasks page + list UX polish shipped (2026-05-15, `docs/specs/director-tasks-and-list-ux-polish.md`).** Three cohesive UX pieces bundled in one ship. (1) **Director task page at `/tasks`** — creator-tier-only single-user task list. New migration `0036_director_tasks.sql` adds the `director_tasks` table (`id`, `team_member_id` FK CASCADE, `title`, `done`, `done_at`, `created_at`) + per-user index. Server actions `addTaskAction` / `toggleTaskDoneAction` / `deleteTaskAction` self-check creator-tier as defense-in-depth even though the sub-layout already gates. Page composes `HeaderBand` + the new `TaskList` Client Component (add input + open / done sections, JS-side sort, optimistic UI via `router.refresh`). TopNav gains a "Tasks" entry with `requiredTier: 'creator'` — hidden for every other tier. (2) **Conditional Slack column on `/clients`** — the "Slack" column added earlier today now renders only when the user has the `needs_review` OR `missing_slack` filter active. Pre-filter visual layout (9-column table) restored when neither chip is engaged; the table widens to 10 columns when either is. The filter chips themselves stay always-visible in the bar. `/clients/[id]` detail page renders missing-Slack pills unconditionally — different concern. (3) **Sticky list filters + Clear-filters polish on both lists.** New `app/(authenticated)/clients/[id]/back-to-clients-button.tsx` (and parallel `back-to-calls-button.tsx`) are Client Components reading the `from` query param and validating it starts with `/clients` (or `/calls`) to prevent open-redirect via crafted URLs. Falls back to bare `/clients` (or `/calls`) on anything else. Row links on both list tables now append `?from=<URL-encoded list path+query>` so the Back button restores the same filtered/sorted view. Existing "Clear" button on `/clients` filter-bar renamed "Clear filters" and now preserves the active search query (search is a different concern from filters per spec). New "Clear filters" button on the Calls filter-bar mirroring the same behavior — wasn't there before. Test suite unchanged (no Python touched; TS surfaces have no test infra). 575 pytest passing total, `tsc --noEmit` + `npm run lint` clean. Migration count bumped to 36; public table count 26. New schema doc `docs/schema/director_tasks.md` + new runbook `docs/runbooks/director_tasks.md` cover the lifecycle + SQL-based recovery paths.

- **Auto-created client lifecycle shipped (2026-05-15, `docs/specs/auto-created-client-lifecycle.md`).** Four cohesive pieces of the "what happens to auto-created clients" story landed in one feature. (1) **Auto-create on new title patterns**: `ingestion/fathom/classifier.py:_classify_by_new_convention` now emits an `AutoCreateRequest` when a post-cutoff `Coaching/Sales Call with {Scott|Lou|Nico}` call has an unresolved external participant — closing the gap the prior cutoff spec left where post-cutoff client calls landed with `primary_client_id=null` and no client row. The pipeline's existing `_lookup_or_create_auto_client` handles the dedup-then-insert. Reason string `"new title convention with unresolved participant"` distinguishes from the legacy Scott-1:1 path. Multi-external case: auto-creates the FIRST unresolved email; others surface via call_participants. (2) **Merge button re-surfaced** on `/clients/[id]` — the existing `MergeClientButton` component + `mergeClientAction` + `mergeClient` + `merge_clients` RPC (migration 0015) all already worked; just needed conditional rendering when `needs_review` is on `tags`. Path A — pure reuse. Page fetches `listMergeCandidates` only when needs_review is present (saves a round trip in the common case). (3) **"Mark as reviewed" button** new at `app/(authenticated)/clients/[id]/remove-needs-review-button.tsx` — confirmation-modal flow, new `removeNeedsReviewTagAction` server action removes `needs_review` from the `tags` column and stamps `metadata.needs_review_cleared_at` for audit. Renders beside the Merge button under the same `needs_review` gate. (4) **Missing-Slack badges** computed read-time on both `/clients/[id]` and the `/clients` list — `MissingSlackChannelPill` + `MissingSlackUserPill` (new in `pills.tsx`) render warn-tier pills when `slack_user_id` is null or no active `slack_channels` row exists. List page gains a new "Slack" column + a "Missing Slack" filter chip. Data layer extended: `getClientsList` now joins `slack_channels`, projects an active `slack_channel_id` onto each row, and applies `missing_slack` filtering JS-side (PostgREST `.or()` can't span column-vs-join cleanly). Auto-create + Slack-hygiene concerns are surfaced separately — a legacy client with broken Slack data shows missing-Slack but not needs_review, and vice versa. Test suite: +5 in `tests/ingestion/fathom/test_classifier.py` covering matched-client-no-auto-create / multi-external-first-wins / no-external-no-auto-create / distinct-reason-string / pre-cutoff-no-new-path (plus the prior spec's "no auto-create" assertion flipped). 575 pytest passing total (was 570). `tsc --noEmit` + `npm run lint` clean. No migrations. New runbook at `docs/runbooks/auto_created_client_management.md` covers the full lifecycle + audit SQL.

- **Call title-convention enforcement shipped (2026-05-15, `docs/specs/classifier-enforce-new-title-convention.md`).** Hard cutoff at 2026-05-18T00:00:00 America/New_York: calls with `started_at` at or after the cutoff only auto-classify as `call_category='client'` when their title matches one of six canonical patterns generated by Zain's booking links — `Coaching Call with {Scott|Lou|Nico}` or `Sales Call with {Scott|Lou|Nico}` (case-insensitive prefix match, trailing context tolerated). Forcing function for the team's adoption of the new booking-link workflow — ad-hoc titles + legacy recurring meetings stop classifying as client post-cutoff, surfacing the gap on `/clients`, in Ella's retrieval, and on `/teams`. Pre-cutoff calls use the prior cascade unchanged; no retroactive reclassification. Implementation in `ingestion/fathom/classifier.py`: new constants `_NEW_TITLE_CONVENTION_CUTOFF` (zoneinfo-anchored for DST safety) + `NEW_CLIENT_TITLE_PATTERNS`; helpers `_matches_new_client_title_convention` + `_is_after_new_convention_cutoff` + `_classify_by_new_convention`; `_classify_by_participants` gained an `allow_client_classification` flag so post-cutoff non-matching titles fall through to internal/external/unclassified without losing the rest of the cascade. New-pattern matches land with `classification_method='title_pattern'` + `classification_confidence=1.0` (above CONFIDENCE_HIGH; the booking-link-generated titles are the strongest signal we have). Scott-1:1 pattern (`30mins with scott`) is implicitly retired post-cutoff — replaced by `Coaching Call with Scott`. Manual override via `update_call_classification` RPC still works for legitimate edge cases. Test suite: +13 across `tests/ingestion/fathom/test_classifier.py` covering pre-cutoff regression, post-cutoff new title (with confidence 1.0 + correct call_type derivation for coaching vs sales), trailing-context tolerance, old-style title rejection, ad-hoc title with known client participant rejection, case-variant + whitespace tolerance, exact-boundary inclusivity, new-title + no-resolvable-client still-client behavior, internal-title still-internal, Scott-1:1 retirement, plus direct helper coverage. 570 pytest passing total (was 557). New runbook at `docs/runbooks/call_title_convention.md` covers the rule + CSM-facing communication + audit SQL for monitoring adoption. Drake's gate (c) is the post-2026-05-18 review of who's actually using the new convention — query `calls WHERE started_at >= cutoff AND call_category != 'client'` to spot stragglers.

- **Teams Meeting Tracker — personal-email exclusion + Nabeel removal shipped (2026-05-15, `docs/specs/teams-personal-email-exclusion-and-nabeel-removal.md`).** Two surgical changes on top of the same-day external-attendee filter spec. Migration 0035 (`nabeel_remove_from_csms.sql`) flipped `team_members.is_csm` to false for Nabeel Junaid — he no longer takes client calls; removes him from `/teams`, the Primary CSM dropdown on `/clients`, and the Primary CSM swap flow on `/clients/[id]`. His 3 active `client_team_assignments` rows stay intact per Drake's direction — affected clients still surface Nabeel as their assigned CSM until manually reassigned. Cron filter extended with personal-email exclusion: `team_members.metadata.personal_emails` jsonb array (no schema change — metadata is freeform) carries the list of non-AIP addresses a team member uses for internal meetings; `_has_external_attendee` now treats them as internal. Loaded once per tick via new `_fetch_personal_emails(db)` helper into a lowercased set threaded down to `_upsert_events`. Today's entry: `huzaifasaeed460@gmail.com` on Huzaifa's row (one-shot SQL UPDATE on `metadata` via psycopg2, dual-verified). One-time cleanup DELETE against cloud dropped 20 stale rows (142 → 122) — all "CSM Sync" internal meetings that survived the old filter because Huzaifa joined from his personal Gmail. Test suite: +3 across `tests/api/test_teams_calendar_sync_cron.py` (personal email treated as internal / real external still kept when personal email present / empty set behaves identically for backward compat). 557 passing total (was 554 pre-spec). Spec hard stops honored: don't reassign Nabeel's existing assignments, Huzaifa-only personal-email entry today, filter rule extension only (no new categories).

- **Teams Meeting Tracker external-attendee filter shipped (2026-05-15, `docs/specs/teams-calendar-external-attendee-filter.md`).** Calendar sync cron now drops events with zero non-`@theaipartner.io` attendees before upsert — OOO blocks, focus time, internal-only 1:1s no longer clutter `/teams`. Filter helper `_has_external_attendee` in `api/teams_calendar_sync_cron.py` lives at module scope and slots into `_upsert_events` alongside the existing cancelled / all-day skip conditions. Case-insensitive on the domain check; resource calendars (conference rooms) explicitly skipped during the external-attendee scan. One-time cleanup `DELETE` ran post-deploy against existing rows that failed the new filter (count + execution surfaced in the report). Test suite: +6 across `tests/api/test_teams_calendar_sync_cron.py` covering empty-attendees / all-AIP / mixed-AIP-and-external / case-insensitivity / resource-calendar-only / attendee-without-email cases. 554 passing total (was 548 pre-spec).

- **Teams Meeting Tracker shipped (2026-05-14, `docs/specs/teams-meeting-tracker.md`).** New head-CSM-and-up dashboard surface at `/teams` showing each CSM's current-week meetings plus Fathom-match status. Two new migrations: `0033_oauth_tokens.sql` (per-team-member OAuth credential storage; provider CHECK pinned to `'google'`; unique index on `(team_member_id, provider)` as the upsert key; FK to `team_members` with ON DELETE CASCADE) and `0034_calendar_events.sql` (cached Google Calendar events; unique index on `(team_member_id, google_event_id)` as the upsert key; secondary index on `(team_member_id, start_time)` for the per-week page query). Both applied + dual-verified post-apply — schema reality + indexes + constraints + ledger registrations + drift check (public tables 23 → 25). Three new env vars in `.env.example` (gate (d), Drake adds to Vercel Production): `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET`, `NEXT_PUBLIC_APP_URL`. Google OAuth2 flow lives at `app/api/auth/google/{connect,callback}/route.ts` (creator-gated; state cookie + nonce validation; upserts the token row with refresh_token on success). `lib/google/oauth.ts` (server-only) owns the TS-side surface: `buildAuthUrl` / `exchangeCodeForTokens` / `refreshAccessToken` / `getValidAccessToken` (the last one reads + refreshes + persists in one call). Parallel Python module at `shared/google_oauth.py` provides `get_valid_access_token` and `_refresh_access_token` for the cron's use; both modules read from the same `oauth_tokens` row. Cron at `api/teams_calendar_sync_cron.py` fires every 30 minutes (`*/30 * * * *`), per tick resolves Drake's `team_member_id` via email + access_tier=creator lookup, mints a fresh token via the Python helper, queries Google's `events.list` for each CSM (`is_csm=true`, not sentinel) with a Mon-Sun EST week window converted to UTC via `zoneinfo.ZoneInfo('America/New_York')` (DST-safe), upserts into `calendar_events` with cancelled / no-`start.dateTime` filtered out. Per-CSM auth failures (401/403) get a `calendar_api_denied` audit row + continue; transport failures get a generic audit row + continue — fail-soft per CSM. Audit pattern matches existing crons (`webhook_deliveries.source='teams_calendar_sync'`). `/teams` page reads from `lib/db/teams.ts:getTeamsThisWeek` which pulls CSMs + calendar_events + candidate `calls` rows for the week (±30 min buffer on edges) + bulk client name lookup, then JS-joins via case-insensitive trim title equality + ±30 min time tolerance with closest-in-time wins. Match attaches `client_name` + signed `minutes_late`. UI renders via `HeaderBand` for the page header, expandable per-CSM cards via a small Client Component (`csm-block.tsx`), `GegPill` for the lateness + API-denied indicators. Empty-state per CSM is `(no meetings this week)` — not hidden. Drake-tier sees a Reconnect Calendar button when the token is missing or refresh failed; other tiers see a muted "Calendar data unavailable" line. `app/(authenticated)/teams/layout.tsx` gates head_csm-and-up (mirrors the Ella sub-layout pattern). TopNav `NAV_ITEMS` extended with `{ href: '/teams', requiredTier: 'head_csm' }`; preview-bypass continues to stub tier=`'creator'` so Playwright verifiers see the link. `vercel.json` gained the python function entry + cron schedule. `lib/supabase/types.ts` extended with `oauth_tokens` + `calendar_events` Row/Insert/Update interfaces. Test suite: +11 across `tests/api/test_teams_calendar_sync_cron.py` (6 — Drake-not-found, OAuth refresh failure, happy path 2 CSMs, partial failure with one CSM denied, cancelled+all-day event filter, sentinel exclusion) and `tests/shared/test_google_oauth.py` (5 — returns stored token when valid, refreshes when expired, raises on no row, raises on http error, proactively refreshes within 60s buffer). Total passing: 548 (was 537 pre-spec). `tsc --noEmit` + `npm run lint` clean. Drake's gates remaining: (d) confirm the three new env vars are set in Vercel Production; (c) post-deploy walkthrough — visit `/api/auth/google/connect` as creator, approve, watch the callback redirect to `/teams` with the token row written, then wait 30 min (or trigger manually via `curl`) and confirm each CSM's calendar events surface on `/teams`.

- **Permissions infrastructure shipped (2026-05-14, `docs/specs/permissions-access-tiers.md`).** Four-tier hierarchical access model on `team_members` plus server-side route gating, Ella audit pages moved to Admin tier. Migration `0032_team_members_access_tier.sql` added `team_members.access_tier text NOT NULL DEFAULT 'csm'` with a CHECK constraint pinning the four values `csm`/`head_csm`/`admin`/`creator`; backfill on apply promoted 6 known users (Drake → creator via email match; Nabeel Junaid → admin; Scott Wilson → head_csm; Lou Perez / Nico Sandoval / Zain → csm explicitly; the other 5 team_members rows including the Gregory Bot + Scott Chasing sentinels stay at the default). Drake's email was confirmed via `SELECT` against cloud `team_members` pre-apply per his correction (work email `drake@theaipartner.io`, not the gmail in the original spec draft); dual-verify post-apply showed the column present + CHECK constraint + ledger entry + 3 rows elevated + 8 at default = 11 total rows + no public-table drift. New TypeScript primitive at `lib/auth/access-tier.ts` (server-only, reads current Supabase user + looks up `team_members` via admin client) + `lib/auth/access-tier-shared.ts` (pure `AccessTier` type + `tierAtLeast(actual, required)` helper importable from Client Components). The `(authenticated)/` layout calls `getCurrentUserAccessTier()` once per page load; a missing `team_members` row redirects to `/login?error=no_team_member_row` with an editorial-warn-tier error banner. New `app/(authenticated)/ella/layout.tsx` gates `/ella/runs` + `/ella/runs/[id]` behind `tierAtLeast('admin')` and redirects CSM/head_csm tier users to `/clients?error=insufficient_access`. `components/top-nav.tsx` receives the resolved tier as a prop and filters `NAV_ITEMS` by `requiredTier` so the Ella link is hidden for users below Admin tier; the preview-mode bypass (`NEXT_PUBLIC_DISABLE_AUTH=true`) stubs the tier as `'creator'` so Playwright verifiers still see every gated surface. `lib/supabase/types.ts` updated to include the new `access_tier` column on `team_members` (TS now compiles clean; ESLint passes). `tsc --noEmit` + `next lint` + `pytest tests/` (537 passing) all green. Production validation is gate (c) — Drake logs in as creator + verifies all routes visible; later validates Scott / Lou see Clients + Calls but no Ella link, and a direct visit to `/ella/runs` redirects. No UI for managing tiers in V1 — changes via SQL until a Settings spec lands. Spec hard stops honored: pre-flight design-system check passed (no drift), `role` column untouched, only Ella moved (Calls + Clients stay open to all authenticated tiers), preview-bypass preserved. Unblocks two queued specs that reuse this primitive: Meeting Tracker for Head CSM, Task Tracker for Creator.

- **Ella passive Gate 4 escalation-keyword bypass shipped (2026-05-14, `docs/specs/ella-passive-escalation-keyword-bypass.md`).** Closes the gap surfaced post-deploy of the escalation-unification spec: three production smoke-test messages ("I want my money back" / "give me my god damn money back" / "I'm really frustrated, I want to talk about canceling my account") with KB similarities 0.22 / 0.23 / 0.28 died at Gate 4 before Haiku could ever see them. New `_ESCALATION_BYPASS_KEYWORDS` frozenset constant in `agents/ella/passive_monitor.py` covers five categories — money/commitment (cancel, refund, money back, charge, billing, contract, …), complaints (frustrated, angry, disappointed, …), crisis/self-harm (kill myself, suicide, want to die, …), quitting/leaving (quit, done with this, wasted my time, …), legal (lawyer, lawsuit, sue you, …). `_has_escalation_bypass_keyword` does a case-insensitive substring scan; on a match Gate 4 lets the message through to Haiku even when no KB chunk passes the 0.30 threshold. **Haiku still decides** — bypass routes to Haiku, it does NOT auto-escalate. The matched keyword is plumbed through `PassiveEvaluation.bypass_keyword` (new field) and `passive_dispatch.persist_passive_evaluation` onto `agent_runs.trigger_metadata.kb_relevance_bypass_keyword` for audit; `/ella/runs` reads it. `ELLA_PASSIVE_KB_RELEVANCE_THRESHOLD` stays at 0.30 for non-keyword messages (spec hard stop: don't lower the threshold globally, bypass is the right lever). Test suite: +12 across `tests/agents/ella/test_passive_monitor.py` (7 — bypass with no KB / bypass with KB / control case / case-insensitivity / multi-word phrase / category coverage parametrized to 5) and `tests/agents/ella/test_passive_dispatch.py` (2 — bypass keyword lands in trigger_metadata / unset omits the field). Total passing: 537 (was 525 pre-spec). Doc updates: `ella.md` Gate 4 bypass paragraph + `ella_passive_monitoring.md` "Gate 4 silently dropping an escalation-worthy message" troubleshooting section + bypass-fired runs SQL query. No migrations, no Haiku prompt edits, no env var, no config table — single Python constant; iteration via PR.

- **Ella escalation unification shipped (2026-05-14, `docs/specs/ella-escalation-unify-and-route-to-scott.md`).** Both reactive (`agents/ella/agent.py:_run`) and passive (`agents/ella/passive_dispatch.py` Haiku-decided + `agents/ella/agent.py:respond_to_passive_trigger` Sonnet-side) escalation paths now route DMs through one shared helper at `agents/ella/escalation_routing.py`. `resolve_escalation_recipients(primary_csm)` reads the new env var `ESCALATION_RECIPIENT_SLACK_USER_ID` (Scott's slack_user_id) and returns a deduplicated Scott-first recipient list; unset → primary CSM only (logged warning, never raises). `fire_escalation_dms(...)` builds the canonical body (`:eyes: Worth a look — <permalink>\n_Ella escalated this. Reasoning: <≤200 chars>_`) and fans out one `shared.slack_post.post_message` per recipient with one `webhook_deliveries` audit row each under the renamed unified source `'ella_escalation_dm'` (renamed from `'ella_passive_escalation_dm'` — `lib/db/ella-runs.ts:fetchEscalationBodies` accepts both labels so historical rows still render). Passive escalations now write an `escalations` row via `agents.ella.escalation.escalate()` (closing the persistence gap — passive previously skipped this), so `/ella/runs` + audit queries see both shapes identically. Prompt edit in `agents/ella/prompts.py` § WHAT YOU ESCALATE: Sonnet's escalation ack no longer carries an `<@U…>` Slack mention (the backend DMs handle notification; an in-channel mention would double-ping); non-escalation conversational mentions remain untouched. Style examples in `docs/agents/ella/ella.md` reshaped to match. `output_summary` on escalation `agent_runs` rows now reads `"escalated via DM; Scott=ok, Lou Perez=ok; escalation_id=<id>"`; dashboard placeholder pattern `/^escalated via DM/i` continues to match so body-suppression still works. Test suite: +12 tests across `tests/agents/ella/test_escalation_routing.py` (11 — recipient resolution + DM fan-out) and `tests/agents/ella/test_agent.py` (1 mention-preservation in non-escalation contexts); existing `test_passive_dispatch.py` escalate-branch tests rewritten to match the unified shape. Total passing: 525 (up from 512 baseline). Drake configures `ESCALATION_RECIPIENT_SLACK_USER_ID` in Vercel as gate (d); validation on `#ella-test-drakeonly` post-deploy as gate (c). No migrations. Forward-only — historical passive escalations stay where they are (no backfill into `escalations`).

- **Gregory wrap day shipped to production (2026-05-14, branch `gregory-csm-visual-fixes` merged into `main`).** Six workstreams landed end-to-end in one Builder session via the design-handoff + spec-cherry-pick flow established in the prior session. (1) **Primary CSM editable on `/clients/[id]` + `/clients` list.** Wraps the existing atomic `change_primary_csm` RPC (migration 0014) + the existing `changeClientPrimaryCsm` Server Action — no duplicate logic. New `EditablePrimaryCsmCell` in `app/(authenticated)/clients/editable-cell.tsx` composes `EditableField` with `variant="enum"` + the new `compact` + `omitEmptyOption` props on the primitive. New helper `listAvailableCsms()` in `lib/db/clients.ts` filters `team_members` by `is_csm=true` + active. Detail-page row uses `compact` to match the height + horizontal rhythm of its plain-text siblings (Email / Phone / Country / Timezone / Start date); list-page cell uses default sizing to match its `EditableField` neighbors. (2) **NPS-enabled + Accountability-enabled toggle pills on `/clients/[id]`.** New `EditableNpsEnabledToggle` + `EditableAccountabilityEnabledToggle` cells wrap a small `PillToggle` base (`<button>` + `GegPill`) with optimistic flip + revert-on-failure + `router.refresh()` on success. Two new dedicated Server Actions (`updateClientNpsEnabledAction`, `updateClientAccountabilityEnabledAction`) wrap `updateClient` with `revalidatePath` on detail + list. (3) **Back-to-clients always goes to `/clients`.** Replaces the `router.back()` + `window.history.length` probe with a plain `<Link href="/clients">`. Filter / sort state on `/clients` persists via URL params. Calls-side back button was already a plain inline Link; no fix needed there. (4) **Action items transfer fix — `lib/db/clients.ts:getClientById` predicate.** Replaced `.eq('owner_client_id', id)` with a `calls!inner(primary_client_id)` JOIN filtered by the joined call's `primary_client_id`. The old predicate only matched items where the *client* was the assigned doer — most action items are CSM-owned (`owner_team_member_id` set, `owner_client_id` null) and silently dropped. Now every action item whose source call has the given client as primary surfaces on `/clients/[id]`. Bug predated yesterday's redesign (git blame on the bad predicate). Same predicate also affects `getClientsList`'s open-action-items count — logged in `docs/known-issues.md` for follow-up. (5) **Send-to-Slack action items wired (dry-run-validated, production cutover pending).** Spec assumed Ella's `@slack/web-api` `WebClient` was already in use; reality is Ella's Slack stack is entirely Python (`shared/slack_post.py`). Builder shipped a 30-line TS direct-`fetch` helper at `lib/slack/post.ts` mirroring the Python helper exactly (same `chat.postMessage` endpoint, same `SLACK_BOT_TOKEN`, same 5s `AbortController` timeout, same `{ok, slackError}` result shape). New `sendActionItemsToSlackAction(client_id)` in `app/(authenticated)/clients/[id]/actions.ts` does a fresh DB read for the channel (via `slack_channels` with the same shape `getClientById` uses) + open action items (via the new `calls!inner` JOIN from fix 4), formats the bulleted mrkdwn message, logs payload JSON to `console.info` (live + dry-run, plus the full body in dry-run), branches on `SLACK_DRY_RUN === 'true'`. Button state machine: idle → sending → sent (3s flash) → gone, or → failed (5s) → idle. Disabled with tooltip when `client.slack_channel_id` is null. New env var: `SLACK_DRY_RUN` — set `true` on Preview for verification; production cutover is the env-var flip (gate (d)). (6) **Ella pre-redesign fixes + visual redesign (Claude Design + Builder).** Extended `EllaRunDetail` with `triggering_message_full_text` (reuses `rawMsgs` already fetched for `thread_messages` — no extra round trip; falls back to `input_summary` when the synthesized trigger row is the fallback). `slack_response_text` was already the full message text — verified, no data-layer change for Ella's response. New `lib/slack/render-mrkdwn.tsx` (~150 non-comment lines) — two-pass tokenizer handling `*bold*` / `_italic_` / `~strike~` / `` `code` `` / ``` ```block``` ``` / `<link|label>` / `<#chan|name>` / `<@user>` (via existing `renderMentions`) / `• bullets` / paragraph breaks. No external markdown library. New `ExpandableMessage` client component wraps the renderer with a 500-char truncate + "Show full message (N more chars) →" / "← Show less" toggle. `SurroundingMessagesSection` removed from `/ella/runs/[id]` per Drake's call; data-layer paths preserved — fix #2 reuses them. Escalation Output column body wired: `agents/ella/passive_dispatch.py:_fire_escalation_dm` now persists the DM body to `webhook_deliveries.payload.body` (one-line dict addition; body construction moved up so it's in scope at audit-payload time). New `fetchEscalationBodies` helper in `lib/db/ella-runs.ts` does a batched fetch by `source='ella_passive_escalation_dm'` + `received_at` window, then JS hash-joins by `(slack_channel_id, triggering_message_ts)` → body. New `escalation_body` field on `EllaRunsListRow`; table cell prefers it over `output_text`. Placeholder string `escalated via DM; csm=...; dm_ok=...` suppressed at the data-layer projection when matched. Forward-only — pre-deploy escalation rows render `—` muted. `renderSlackText` extended with `stripSlackLinkSyntax` so Slack permalinks render cleanly. Then the **full Ella visual redesign** by Claude Design (single-file HTML mock at `Gregory Ella Redesign.html`, archived to git history) + Builder: list page restyled with 4-card gold-bordered metric strip, editorial filter bar (date range + Channel + Status — Triggers filter explicitly excluded per Drake), native `<table>` matching the Calls + Clients pattern. Detail page restyled with hand-rolled editorial header, backlink in mono caps gold, 420 / 1fr grid (LEFT: Context + Haiku decision + Escalation; RIGHT: Triggering message + Ella's response, each in a `geg-gold-box` containing a `geg-slack-msg` block). "Show more" preserved on both message sections. Pills migrated from shadcn Badge to `GegPill` — new `TriggerTypePill` exported for the detail header. New `.geg-slack-msg` paragraph + list + inline-mark CSS rules in `app/globals.css` for the `Mrkdwn`-rendered output. Plus an end-of-day Ella prompt update: `agents/ella/prompts.py` gains one line — "Clients meet with their advisors via a calendar booking link." — inserted in the WHO YOU ARE section. Playwright visual verification on the deploy preview was the durable workflow for every visual surface (six harnesses landed at `scripts/verify-csm-visual-fixes.ts`, `verify-action-items-transfer.ts`, `verify-send-to-slack.ts`, `verify-ella-pre-redesign-fixes.ts`, `verify-ella-escalation-output.ts`, `verify-ella-redesign.ts`). Pattern is now the established default for visual work: preview branch + `NEXT_PUBLIC_DISABLE_AUTH=true` on Preview env only + Playwright harnesses that screenshot + assert. End-of-day cleanup ran: shipped specs + reports deleted, design-mock HTMLs archived to git history, merged feature branches pruned (preserving `promethean-shell`).

- **Gregory native `<select>` polish (2026-05-12).** Post-reskin follow-up per `docs/specs/gregory-native-select-polish.md`. The inline-edit cells on `/clients` (Status / Journey stage / CSM Standing / Trustpilot) and the Lifecycle + Adoption sections on `/clients/[id]` all route through one `<select>` element in `components/client-detail/editable-field.tsx`. Native rendering of that element didn't pick up the editorial-dark theme — light closed state, cell-expansion on open, persistent focus ring after blur. Fixed by adding a `.geg-select` class to the `<select>` plus a `.geg-editable-display` class to the display-mode wrapper, with theme-scoped CSS in `app/globals.css` under `[data-theme="gregory-editorial"]`: `appearance: none` + custom SVG chevron + dark editorial bg + locked 36px height + `:focus-visible`-only outline rules so mouse clicks don't leave a ring while keyboard tabbing still surfaces accent focus. `<option>` elements styled best-effort (Chrome / Firefox respect; Safari mostly ignores per native limits). No data layer changes; the `commit(draftOverride)` stale-closure pattern in `EditableField` untouched per spec.
- **Gregory full redesign shipped to production (2026-05-13, merged into `main` from branch `gregory-redesign-part-1-foundations`).** Visual + UX pass across Calls (list + detail), Clients (list + detail), and Ella audit (list + run detail), built on a new Part 1 foundation primitive set. Accent flipped from electric blue to gold (`--color-geg-accent: #a08850`) with five derived variants (-strong, -dim, -border, -fill, -focus); sentiment palette decoupled from accent and pinned to literal pos/warn/neg tokens (`--color-geg-pos #7a9e6f`, `--color-geg-warn #c9a657`, `--color-geg-neg #c97766`) so future accent swaps don't drag sentiment with them. Typography pairing locked in: Newsreader (serif display) + JetBrains Mono (IDs, eyebrows, dates) loaded via `next/font/google` and exposed as `--font-geg-serif` / `--font-geg-mono`. Part 1 foundation primitives live at `components/gregory/`: `header-band.tsx` (eyebrow + serif H1 + count), `sentiment-pill.tsx` (POSITIVE / MIXED / NEGATIVE, mono caps + 6px dot), `geg-pill.tsx` (5-tier generic pill: pos / warn / neg / muted / gold — backs the Clients vocab pills), `inline-editable-field.tsx` + `inline-editable-action-item-row.tsx` (display + edit primitives with optimistic-update contract), `empty-state-aware-section.tsx` (auto-skips section render when slot is empty), `diagnostics-collapse.tsx` (collapsible Diagnostics block on detail pages). Calls (`/calls`, `/calls/[id]`): list rebuilt with raw `<table>` + 6 columns (Date / Title / Primary client / CSM / Sentiment / Duration); pre-filtered server-side to `category='client'`; gold-border row dividers on `tbody td` (not `tbody tr` — separate-mode borders); detail page is a two-column gold-box layout (Data + Action items left, Call review right) with the inline-edit + Confirm + hard-delete action-item flow wired through `commitPendingActionItemChanges`. Clients (`/clients`, `/clients/[id]`): list keeps all 9 existing columns (Full name / Status / Journey stage / Primary CSM / CSM standing / NPS standing / Trustpilot / Health / Meetings · mo) but applies the new visual language — `EditableField` cells render their pill via the `displayValue` prop so inline-edit + pill display coexist; Health cell is number + 36px gradient bar with positioned indicator. Detail page rebuilt around the mock's 5-box layout (Details + Standing left; Health + Action items + Recent calls right); legacy 7-section structure (Financials / Profile / Adoption / Notes / etc.) dropped from this page — data still in the DB and can be re-surfaced if needed. Ella (`/ella/runs`, `/ella/runs/[id]`): list polished with the editorial table chrome + SentimentPill column; detail page reorganized into Triggering message → Ella's response → Surrounding messages → What Haiku decided → Escalation → Diagnostics ordering; surrounding-messages unified to last-5-channel anchored on trigger_ts. Sentiment classifier shipped at `agents/call_reviewer/sentiment_classifier.py` — Haiku classifies each call_review's `sentiment_arc` into green / yellow / red, written to `documents.metadata.sentiment_tier`; backfill via `scripts/backfill_sentiment_tiers.py` (Drake-run). CSM-on-calls data path: `lib/db/calls.ts` extended to surface the active `primary_csm` via the `client_team_assignments` join (mirrors `lib/db/clients.ts:getClientsList`). Visual-verification harness: `scripts/verify-calls-preview.ts` + `scripts/verify-clients-preview.ts` are Playwright scripts that load the Vercel preview deployment, screenshot the list + one detail page each, and save to `scripts/.preview/`. Preview auth bypassed via the env-gated `NEXT_PUBLIC_DISABLE_AUTH=true` (Preview env only, never Production) — branch `app/(authenticated)/layout.tsx` returns a stub user when the flag is true. Design workflow established and now the default for visual work: Drake + Director ideate in chat → Director writes a Design prompt → Claude Design (separate claude.ai session, GitHub MCP authorized) produces annotated single-file HTML mocks at `docs/working/<Surface> Redesign.html` → Director writes a UI spec referencing the mock → Builder implements + Playwright-verifies + reports. Documented in the new `docs/runbooks/design-handoff.md`. Send-to-Slack on the Clients detail action-items box is a placeholder pending the dedicated spec (Next Session Priorities item 2).
- **Gregory editorial-dark visual reskin shipped to production (2026-05-12, merge commit `13bb288`).** Branch `gregory-editorial-skin` merged to main via `--no-ff` per `docs/specs/merge-gregory-editorial-skin-to-main.md`. Pure visual reskin — no layout, surface, column, section, or information changes. Same editorial-dark aesthetic as Promethean but with **electric blue (`#0066FF`)** as the accent (Promethean uses yellow-green). Implementation: a new `[data-theme="gregory-editorial"]` block in `app/globals.css` overrides the shadcn `--background` / `--card` / `--primary` / `--border` / `--ring` tokens to the editorial palette, so every shadcn primitive (Card / Button / Input / Table / Badge / Select) auto-flips under the scope without touching individual component files. Parallel `--color-geg-*` tokens cover patterns shadcn doesn't express. Theme-scoped CSS overrides remap the hardcoded Tailwind palette classes Gregory's pills use (bg-emerald-100, bg-amber-100, bg-rose-100, bg-zinc-100, bg-sky-100, bg-blue-100, bg-white, plus text + border variants) onto the editorial palette without modifying any pill component code. Instrument Serif + Inter hoisted to `app/layout.tsx` via `next/font/google` (option (a)) and exposed as `--font-prom-serif` / `--font-prom-sans` at the root so both Gregory's editorial theme and (when its branch lands) Promethean's theme can share the same CSS variables. `data-theme="gregory-editorial"` applied at `app/(authenticated)/layout.tsx` wrapper + `app/login/page.tsx` wrapper. TopNav restyled with serif "Gregory" wordmark + LIVE pulse dot + electric-blue active-nav underline + dark editorial chrome. Login form restyled with masthead pattern (LIVE pulse + small-caps wordmark + 64px serif "Sign in." + italic deck + form on hairline-bordered card). Page headers across `/clients`, `/clients/[id]`, `/calls`, `/calls/[id]`, `/ella/runs`, `/ella/runs/[id]` rebuilt with the editorial eyebrow-plus-serif pattern. Client detail Section primitive restyled (serif section title at 22px, muted chevron) — collapsible behavior unchanged. No data layer touched (no RPC, Server Action, migration, vocab, or schema change). Build clean post-merge: 9 routes, no TypeScript / ESLint / React warnings. Vercel auto-deployed from the push; production smoke is Drake's gate (c). Original spec at `docs/specs/gregory-editorial-dark-skin.md`; full implementation report at `docs/reports/gregory-editorial-dark-skin.md`. Feature branch `gregory-editorial-skin` retained until Drake confirms production green; then deleted per `docs/specs/merge-gregory-editorial-skin-to-main.md` Step 4.
